# 产品记录

## 已确认目标

创建“教研联盟管理平台”项目，用独立 Git 仓库管理，并同步至 GitHub。

## 本次范围

- 在 active 工作区的 `apps/` 下建立独立项目。
- 建立项目说明、产品记录、开发日志与协作规则。
- 初始化 Git，使用 `main` 分支并完成初始提交。
- 在当前 GitHub 账号下创建私有仓库，配置 `origin` 并推送。
- 验证远端可见性、本地与远端提交一致性、工作区状态。

私有仓库是本次采用的隐私默认值。GitHub 同步已获用户授权；业务系统部署、外部服务付费和真实业务数据上传不在本次范围内。

## 本次验收标准

1. 项目目录及基础文档存在，Git 根目录是本项目目录。
2. Git 有有效提交，默认分支为 `main`。
3. GitHub 私有仓库存在，`origin` 指向该仓库。
4. 本地 `HEAD` 与远端 `main` 的提交一致，工作区干净。

## 后续需求梳理

以下事项尚未确定，不构成功能承诺：

- 联盟的组织方式、学校数量及核心使用者。
- 首个需要解决的教研管理场景及当前操作流程。
- 管理员、学校负责人、教研组长、教师等角色是否需要及其权限。
- 联盟成员、教研活动、资源与成果等候选功能的优先级。
- 数据隐私、部署方式、预算和首版功能验收标准。

## 技术状态

尚未选定技术栈、数据库、托管服务或第三方服务。明确首版场景后再确定技术方案。
