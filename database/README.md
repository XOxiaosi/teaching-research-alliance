# 数据库迁移

迁移文件按数字前缀顺序执行，目标数据库为 PostgreSQL。首个迁移只建立身份、账户、组织、职责和审计的基础表；费用、分配和不可变账本在 DEV-003–DEV-006 中追加，不在首个迁移里预先制造不完整的业务表。

本地开发只允许使用合成数据。当前环境没有 PostgreSQL 客户端或 Docker，因此本阶段对迁移执行做静态结构检查；实际空库迁移和恢复演练在数据库环境建立后执行并记录。
