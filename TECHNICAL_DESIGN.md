# 教研联盟管理平台｜技术设计说明

## 1. 文档职责与设计边界

本文说明如何实现 [PRODUCT.md](PRODUCT.md) V1.19 中 F01–F14，是开发前的技术基线，不代表功能已开发、部署或验收。产品语义、计算口径与待决定事项以 PRODUCT 为唯一事实源；本文只在已确认边界内选择实现方案。执行进度与验证证据记录在 [PROJECT_LOG.md](PROJECT_LOG.md)。本轮按 [REQ-022](docs/requirements/REQ-022-场地看板与学业规划导师.md)（2026-09-18）同步独立场地看板、场地选择与收入、学业规划导师介绍池分配；既有账户、提现、现金工资及最高比例权限继续保留。

已确认的终端边界：主要平台为**微信小程序**，同时提供手机浏览器和电脑浏览器使用的响应式网页。微信小程序与 Web 提供一致的业务能力，共享后端、权限、计算和结算结果，不在客户端复制结算规则。

本设计坚持以下边界：

- 采用 TypeScript 模块化单体和一个 PostgreSQL 主数据库，按约 400 名老师、2,500 名学生设计容量；该规模没有支持微服务拆分的证据。生产方案必须在采购前以不超过 300 元/月的预算重新核价，本文不购买资源、不部署也不写业务代码。
- 原始业务记录、派生结算状态和用户视图分层保存。原始事件不因重算或更正被覆盖；已结算明细不原地修改。
- 业务主键使用不可变 UUID。老师昵称唯一且不可修改，数据库关系一律引用 UUID。
- 表1–表8是业务视图和 Excel 工作表概念，不直接等同于八张数据库表。
- Excel 是管理端可读、可下载的完整业务数据备份。D20 已确认本期**不需要 Excel 导入**；数据库灾难恢复另用数据库备份，二者不能互相冒充。
- 报销和奖金保持系统账户间内部划拨：发起账户扣款、目标个人账户加款，同一事务立即生效。工资以现金发给老师，财务确认已支付后仅在老师个人账户扣除等额欢乐豆，与课时分配独立；不生成工资双侧内部划拨，也不新增现金出资账户的欢乐豆扣款。系统不接银行或其他支付渠道。提现必须足额且为正，提交即自动通过扣豆；用户填银行卡账户和姓名后不可改撤，财务在系统外转账并标完成、不二扣。完成前财务撤回只返豆一次，完成后纠错走调账；D25已关闭。
- 用户期望两周完成全量开发，这作为排期约束和风险提示，不写成工程保证；未关闭的计算口径会直接影响可交付范围。

## 2. 技术方案

### 2.1 代码结构

采用 TypeScript monorepo，建议按以下边界组织：

```text
apps/
  miniapp/        Taro + React，微信小程序
  web/            React 响应式网页看板（手机/电脑共用）
  api/            NestJS REST API，模块化单体
  worker/         实时月度/财年重算及每周/手动导出任务
packages/
  api-contracts/  OpenAPI 生成的请求/响应类型与错误码
  domain/         纯计算函数、状态约束、金额与费率值对象
  ui-tokens/      颜色、间距、字体等共享设计令牌
  test-fixtures/  仅含合成数据的测试样例
database/
  migrations/     前向数据库迁移
  seeds/          本地/测试环境合成数据
docs/
```

小程序和网页可以复用 API 类型、校验模型和无平台依赖的展示逻辑，但分别维护页面组件。Taro 遵循小程序的组件、API 和路由规范，并支持 React；这适合微信小程序主端。网页看板使用标准 React DOM，以免桌面表格、键盘操作和大屏布局受小程序组件限制。两端共享代码不以牺牲各终端体验为代价。

微信小程序和网页端都承接 F01–F12 中账号获准使用的完整业务能力；页面布局分别适配触屏和桌面，但使用同一 OpenAPI、权限矩阵和状态机。

### 2.2 运行组件

| 组件 | 职责 | 关键约束 |
|---|---|---|
| 微信小程序 | 登录、角色选择、个人业务、移动审批、个人看板 | 不保存结算真值；每次写入都调用 API |
| 响应式网页 | 手机/电脑看板、管理配置、大表格、Excel 备份 | 手机与电脑为同一 Web 应用的响应式布局 |
| API | 身份、权限、业务写入、查询、结算编排、审计 | 服务端校验范围；客户端角色/金额均不可信 |
| Worker | 实时整月/财年重算、周备份及异步Excel、重试任务 | 与 API 复用 domain；从事务型任务表取任务 |
| PostgreSQL | 唯一业务真源、事务、约束、审计、任务队列 | 金额使用精确十进制，不使用浮点数 |
| 私有文件存储 | Excel文件、发票/付款凭证图片原件、备份产物 | 本地开发使用临时私有目录；原件、历史业务数据和成功备份永久保留；生产对象存储容量与费用部署前核算 |

首版不引入独立消息队列、搜索引擎或数据仓库。后台任务存入 PostgreSQL `job` 表，worker 使用行锁领取；实际负载证明不足时不增加额外基础设施。

### 2.3 后端业务模块

单个 API 部署单元内保持清晰模块边界：

1. `auth`：手机号密码登录、管理员重置、会话、角色切换。
2. `identity-org`：个人/公司业务/场地账户、三类访问角色、总部—分区—校区关系、校区公司账户、现行十类分润及旧Q/C历史类别和数据范围。
3. `academic-calendar`：学年、period、普通/寒暑假特殊周、结算月和录入截止。
4. `referral-student`：按老师/课程独立的学生生源记录、复制推送、接收、21日归档与再激活事件。
5. `weekly-fee`：按计划周录入、行级校验、版本修订和原期补差。
6. `rate-policy`：基础费率、抬升区间、封顶、绩效分润版本和财年影响预览。
7. `calculation-settlement`：实时整月重算、财年独立账簿、不可变版本和delta；余额跨年保留、期初承接上年期末但不再记一笔收入。
8. `finance-document`：提现及报销/付款单据的有效性、审批、内部划拨和不可变冲回。
9. `compensation`：现金工资计划与支付确认扣豆、公司统一奖金内部划拨、医社保/公积金及项目扣费的月度待办与确认扣豆。
10. `reporting`：权限看板、表1–表8视图、财年范围查询。
11. `export-backup`：每周/手动 Excel 快照任务、清单、文件和下载授权。
12. `audit`：追加式操作日志、请求追踪和安全审计。

模块之间通过应用服务调用和数据库外键协作，不直接让控制器跨模块写表。结算模块只消费已确认输入和已发布配置版本。

## 3. F01–F14 实现映射

| 功能 | 实现模块 | 主要写模型 | 用户读取视图 |
|---|---|---|---|
| F01 登录、角色、权限 | `auth`、`identity-org` | `user_account`、`role_assignment`、`session` | `my_roles`、授权后的角色主页 |
| F02 教师资料与归属（表1） | `identity-org` | `person`、`teacher_profile`、`organization_unit`、`person_relationship` | `v_table1_teacher` |
| F03 生源、接收、课时费（表2） | `referral-student`、`weekly-fee` | `teacher_student_record`、`referral_case`、复制/状态事件、`weekly_fee_entry` | `v_table2_practice` |
| F04 动态费率 | `rate-policy`、`calculation-settlement` | `rate_policy_version`、`rate_tier` | 生效版本和月度试算说明 |
| F05 绩效配置（表8） | `rate-policy` | `split_policy_version`、`split_policy_rate` | `v_table8_split_config` |
| F06 单笔分配 | `calculation-settlement` | `calculation_run`、`allocation_line` | 单笔分配明细和公式输入快照 |
| F07 月度汇总（表3） | `calculation-settlement`、`reporting` | `settlement_period`、`ledger_entry` | `v_table3_monthly_income` |
| F08 财务单据（表4） | `finance-document` | `finance_document`、状态事件、入账记录 | `v_table4_finance_document` |
| F09 工资奖金（表5） | `compensation` | `salary_eligibility`、`base_salary_schedule`、`salary_payment`、`compensation_item_definition`、`compensation_entry` | 总部财务/管理员可查工资资格、现金及个人扣豆；`v_table5_payroll_bonus`按当前视角分离工资与奖金字段 |
| F10 医社保、公积金与扣费（表6） | `compensation` | 个人普通扣费与财务职务账户承担的医社保/公积金分列，到期生成待办后确认扣豆 | `v_table6_deduction` |
| F11 最终结算（表7） | `calculation-settlement`、`reporting`、`finance-document`、`compensation` | 不可变 `ledger_entry`、`balance_adjustment`、结算快照 | `v_table7_balance`、年汇总、人工调账明细 |
| F12 管理与报表 | 全部业务模块、`reporting` | 配置版本、审计记录 | 管理网页和范围内报表 |
| F13 微信小程序与手机/电脑网页 | `miniapp`、`web`、统一 API | 不新增业务真源 | 各角色移动页、响应式看板 |
| F14 Excel 完整业务备份 | `export-backup`、`worker` | `export_job`、`export_artifact` | 管理端导出任务、校验清单与下载 |

## 4. 身份、角色与服务端数据范围

### 4.1 身份模型

- `person.id`：不可变 UUID，所有业务外键使用它。
- `person.nickname`：产品要求的唯一业务昵称，数据库设唯一约束并禁止修改；停用或离职后也不释放该昵称。
- `user_account.phone`：规范化后唯一；密码保存为自适应加盐哈希，绝不保存明文。管理员重置密码后撤销既有会话。
- 一个自然人使用一个手机号登录账号；“主/子账号”只指角色视图，不增加独立登录凭据。手机号规范化唯一，人员主键不可变；变更手机号不重新绑定其他人的钱包。
- 登录账号`user_account`关联自然人`person_id`；结算账户以`owner_type=PERSON|COMPANY|VENUE`区分个人、公司业务与独立场地资金。每自然人一个PERSON结算账户，个人所有收入永久归集到该账户并按收入/职责类别分列。校区、职务及联盟业务资金使用独立COMPANY账户，每场地使用独立VENUE账户，公司账户不放登录凭据；经个人登录和当前任职授权操作。任职权限移交不整体迁移业务资金或个人余额；组长生效当周及后续费用仍按F02差额重算原/新任个人，更早周保留。D24已关闭。
- `employment_status` 与登录状态分离。人员离职后禁用账号和撤销会话，但保留人员、角色历史、业务记录和结算关系。
- 微信小程序只是主要客户端，现阶段登录仍按 PRODUCT 使用手机号和密码。微信 `openid` 不是人员真源；未来如需微信账号绑定，作为可撤销的辅助身份另表保存。
- 会话使用短期访问令牌和可撤销刷新会话。网页刷新凭据放入 `HttpOnly`、`Secure`、合适 `SameSite` 的 Cookie，写请求配置 CSRF 防护，并用明确 CORS allowlist 限制来源。小程序只按平台能力保存完成会话所需的最小短期凭据，不声称平台本地存储具备硬件级或应用自带加密保障；敏感长期秘密不落客户端。日志不得记录密码、完整令牌或银行卡号。

学业规划为专门招生、不授课的个人业务身份，沿用个人基础登录账号；它不是可任命职务，不创建独立登录权限角色。记录推荐时的输出方个人业务身份快照，供推荐分配追溯，不能用当前职务名称倒推历史身份。

### 4.2 角色与范围

注册请求仅创建普通账号及TEACHER/SELF基础权限；拒绝客户端提交的管理角色、特殊职责、特殊分润身份或任意数据范围。除老师自建场地及其限定只读查看授权外，特殊职责任命由SYSTEM_ADMIN专用接口分配，记录授予/撤销人、时间、范围和版本；用户资料更新接口不得写授权表。普通注册不能自行指定其他人的收款账户或绑定公司账户；不自动创建授课/分润关系，无关联业务时本人视图为空。规范化手机号、唯一昵称及必要本人资料按F01/F02校验，账号、人员和所需初始账户映射原子创建；每自然人创建唯一PERSON账户，并以数据库唯一约束防止重复。失败不留半成品，重复请求不新建账户。

登录权限只有三类：`TEACHER`（老师）、`COMPANY_MANAGEMENT`（公司管理层）、`SYSTEM_ADMIN`（系统管理员）。采用 RBAC + 能力 + 数据范围：`role_assignment(user_account_id, access_role, scope_type, scope_id, valid_from, valid_to)`，其中 `scope_type` 支持 `SELF`、`GROUP`、`CAMPUS`、`REGION`、`GLOBAL`；场地业务另以`VENUE + venue_id`绑定所有者/只读查看授权。

当前十类分润为授课、介绍人净额、学业规划导师、校区咨询管理费、教研组长、授课指导导师、场地、平台财务、分区财务及联盟动态。它们是业务类别，不是登录角色；分区默认1%仍由管理员可调，收款不授予总部财务权限。校区咨询管理费沿用唯一CONSULTATION_CAMPUS_FEE，旧Q/C类别与ledger按历史版本保留。规划导师份额来自10%介绍池内部拆分，介绍池仅为计算基数不能再入账；个人所得进本人PERSON账户，场地收入进VENUE账户，其他公司业务收入留对应账户。

REQ-020职责边界：组织层级仍为总部→分区→校区。分区财务保留任命、兼任、本人基础账号、职责切换、自身分润和本区人员名单/营收查看，撤销REQ-018的分区直接调配人员权限。换校区、换指导老师、换教研组长等关系变更统一由SYSTEM_ADMIN执行；分区视角即使兼任管理员也须先切管理员才能修改。换校区仅联动对应校长，组长和导师关系保持不变；场地已独立于校区，按费用所选场地快照收款，调校区不自动换授课场地。分区名称含“财务”不授予总部工资、付款、报销审批、调账或导出能力；其他本人业务与独立授权职责仍按当前视角执行。

REQ-019最高权限：所有分配参数（默认比例、Q、基础费率、档位、上限及表8个别配置）的创建、修改、复制、批量设置和发布仅允许当前SYSTEM_ADMIN视角；这是正式规则，不可下放给其他职务，D28已关闭。即使兼任管理员，当前处于非管理员职责视角也不能写比例，须切换管理员后操作。

运营校长、分区财务、场地运营、教研组长仅可读取本人当前职责对应的分配比例，不得返回团队成员比例、其他岗位比例或完整分配表。“本人比例”按当前职责任命（场地为本人所有权）及分配类别取对应份额，不因挂靠某授课老师就返回该老师六项完整配置。校长读取所任校区职责对应的管理费比例，不代表管理费转为校长个人收入；管理费2%只在学业规划推荐时产生。兼任者每次仅看当前职责的份额，切SYSTEM_ADMIN才可看全量配置。本条仅明确这四职务的比例读取范围，不擅自扩张到其他未提岗位；所有非管理员不能修改比例的规则对全部岗位适用。统计收入及人员名单查看保留，人员关系变更按REQ-020仅管理员办理。

服务端为四职务生成只含获准比例的响应模型，覆盖报表、成员列表/详情、历史配置、错误回包、搜索/同步及嵌套allocation对象；在序列化前剔除未授权比例字段，不能先返回完整对象再由前端隐藏。通用费用校验可返回稳定错误码及获准修正提示，不能在错误details中夹带其他岗位比例或完整表8。

资料权限与业务动作权限分开建模，例如 `VIEW_PERSON_BASIC`、`VIEW_SENSITIVE_FULL`、`VIEW_SENSITIVE_MASKED`、`EDIT_PERSON_MASTER`、`CREATE_WEEKLY_FEE`、`CREATE_FINANCE_DOCUMENT`、`APPROVE_FINANCE_DOCUMENT`、`CREATE_BALANCE_ADJUSTMENT`、`APPROVE_BALANCE_ADJUSTMENT`、`VIEW_BALANCE_ADJUSTMENT`、`EDIT_COMPENSATION`、`CONFIGURE_RATE`、`EXPORT_FULL_BACKUP`。非系统管理员对人员主资料仅按授权读取；人员校区、组长、导师等关系仅管理员可改。这不撤销老师录课时费/单据，也不撤销获授权财务人员审批、录工资和调余额；所有Excel导出（包括单表报表与全量备份）只授予系统管理员。现阶段获授权总部财务能力使用 `GLOBAL` 数据范围，可办理全公司范围内各项已授权业务；个人提现只能本人发起；场地提现仅该场地所有者发起，受邀查看者不可提，不能因财务全局范围替他人申请或扩展为任何职务可提。总负责人通过 `COMPANY_MANAGEMENT + GLOBAL` 表达，不新增第四种登录角色；其可按既有权限查全公司一般财务记录；工资资格、配置、现金及扣豆明细仅当前总部财务或SYSTEM_ADMIN视角可查，不能因总负责人或其他GLOBAL查看范围自动获得。分区/校区管理按各自获授权数据种类查询，分区财务可查看自身分润、校区营收聚合及区内人员名单，不含关系写入或其他老师个人账务。公司管理层可查获准范围的历年数据，老师只查当前财年。系统管理员可逐人配置兼任能力和范围。

敏感资料采用字段级响应契约：总负责人、系统管理员和获授权财务在全公司范围内可查看完整手机号、银行卡、发票和付款凭证；另获人员资料查看能力的分区/校区管理只在各自范围查看脱敏手机号和银行卡，不返回完整值；分区财务可读取区内名单所需最小人员资料，不因该能力返回银行卡或工资明细；老师只查看本人资料及完成获准业务所需的最小字段。服务端按职责与数据范围直接选择 full/masked/minimal schema，不能把完整值发给客户端再隐藏。完整Excel导出仍只有系统管理员可发起和下载。

系统管理员及经实名授权承担 `SYSTEM_ADMIN` 职责的开发者账号可获得人工调账能力；“开发者”不是隐藏角色、共享账号或绕过审计的后门。每次授权保留授权人、被授权自然人、期限、能力和范围。人工调账按操作时选定职责决定路径：财务职责提交后进入管理员审批，不能自行通过；系统管理员及实名授权开发者以 `SYSTEM_ADMIN` 职责提交时免审，但必须完整留痕并显式标记 `NOT_REQUIRED`，不能伪造成已审批。

每个 API 同时校验：

1. 会话是否有效；
2. 当前访问角色是否属于该账号；
3. 该角色是否拥有资源动作权限；
4. 目标行是否处于授权数据范围；
5. 操作是否满足业务状态与乐观锁版本。

右上角角色切换列表从服务端有效授权获取，包含基础普通角色及管理员已分配的特殊角色/职责；切换请求重新验证角色/职责归属，成功返回新授权上下文版本及白名单首页路由，前端自动跳转专属页面。撤销授权后旧上下文立即失效。角色切换只改变当前会话上下文和界面导航，不扩大账号已经拥有的授权。账户类型、访问角色、分润身份三者不得混用。查询必须由服务端拼入范围条件；隐藏按钮不是权限控制。导出任务再次按服务端权限和范围构造快照，不能接收客户端传入的任意人员 ID 列表。


### 4.3 独立场地与学业规划导师

REQ-022将场地从表1固定场地人/校区职务关系中独立出来。老师可以自建场地，创建成功后本人账号立即出现该场地独立看板；同名场地按不同venue_id区分，不合并刘、周、王等不同创建人的场地。创建者可选择老师授予该场地只读查看权，服务端分别校验`VENUE_OWNER`与`VENUE_VIEWER`；只读授权不含提现、修改比例、转授权或改他人资料。自建场地及查看授权是明确业务能力，不授予公司管理权限，不能通过角色配置反向限制为仅管理员可建场地。

接收学生时由接收老师选择真实存在的授课场地，可选本人默认场地并后续变更；一人可建多场地，需明确实际默认venue_id，未创建不能虚构“自己场地”或账户。老师校区及原表1场地负责人不决定该笔收入。每笔费用冻结venue_id、场地关系版本、VENUE收款账户及5%实际比例；自己的场地同样从授课剩余承担5%并进入独立场地余额，不因同人免除或并入个人余额。场地修改从变更当周起重算当周完整累计费用及后续已录费用，更早周不变；旧场地差额冲回、新场地差额入账，保留全部原/新版本，不按周内天数拆分。场地独立账户有本人所有者提现入口，只能提该场地余额，受邀查看者无提现权。选用目录范围及查看者字段边界待D35，不默认全公司可选或可见。

学业规划导师是新增管理职责，由管理员任命并维护旗下规划师关系，与授课指导导师7%使用不同类别和关系表。本人招生拿10%，不再自给2%也不递归向上级分配。旗下规划师介绍10%池内按F的2个百分点给导师，规划师净额8%、导师2%；无导师管理关系仍10%，该条件按“管理下的”解释保留。不先给规划师全10%再多记导师2%，也不从授课老师另扣2%。规划导师自己招生沿用规划招生咨询管理费2%的同口径解释；所有比例仅管理员可改。管理查看字段与规划师换导师生效粒度待D36，不因“管理”开放工资、其他个人余额或团队完整比例。

## 5. 数据模型与表1–表8映射

### 5.1 通用字段和类型

所有可变主记录包含 `id uuid`、`version bigint`、`created_at timestamptz`、`created_by uuid`、`updated_at timestamptz`、`updated_by uuid`。并发更新使用 `WHERE id = :id AND version = :expectedVersion`，成功后版本加一。

- 所有欢乐豆账务金额以“豆”展示，`1 豆 = 1 元`；现金工资实发事实另存精确金额并以“元”展示，对应扣豆为独立欢乐豆账务字段。原始和入账金额使用 `numeric(20,2)`，费率使用 `numeric(12,9)`，中间计算使用 `numeric(38,18)`；禁止 JavaScript `number` 承担财务计算。确定后的各项份额先精确计算，再全部向下取整至 `0.01` 豆；剩余分按小数余数从大到小逐分分配，相同余数按介绍人净介绍费、学业规划导师分润、联盟动态、教研组长、指导导师、场地使用费、校区咨询平台费、平台财务、分区财务、授课老师的固定顺序决胜；动态负项及其尾差/上限待D30，不能按旧“九项均非负”强行截断。算法仍须可重放且总额等于F。API 和 Excel 中金额使用十进制文本；欢乐豆账务金额明确单位为豆，现金工资实发金额明确单位为元，两项不得混作分润收入。
- 所有可配置费率/分润比例在API输入边界按百分数最多两位小数校验，抬升按百分点同精度，既定负抬升仍合法；转换内部比例后保存精确十进制，例如6.25%为0.0625。数据库可保留更高精度的类型，不代表允许输入更多位；拒绝6.251%等超精度输入，禁止先舍入。各比例取值范围、合计上限与当前封顶规则不因精度统一而改变。
- 月份保存为月初 `date`，周区间保存 `date` 起止值，时间点保存带时区时间；业务时区固定为Asia/Shanghai，与客户端所在时区无关。
- 角色、状态、类型使用受约束代码值；中文文案仅为展示层，不作为关联键。
- 真实姓名、手机号、学生姓名、银行卡、发票和付款凭证信息按敏感字段分级；银行卡号加密保存，并按 full/masked/minimal 服务端响应契约展示。

REQ-020增加明确的数据范围：运营校长可查本校区、教研组长可查关联老师、授课指导导师可查指导对象的学生名单、逐笔费用、个人余额和非工资收入。服务端将人员范围与数据种类同时校验，工资字段/关联扣豆明细及四职务比例限制继续过滤；这些查看权不自动授予改人员、改比例、工资或导出能力。

### 5.2 表1：教师信息

| 实体 | 关键字段 | 约束/关系 |
|---|---|---|
| `person` | `id`、`nickname`、`legal_name`、`status` | `nickname` 唯一，ID 不可变 |
| `user_account` | `person_id`、`phone_normalized`、`password_hash`、`login_status` | person_id与手机号均唯一，一个自然人一套登录；公司结算账户不放登录凭据 |
| `teacher_profile` | `person_id`、`primary_business_identity`、`grade_subject`、`region_id`、`campus_id`、`employment_status` | 每人一分区一校区，region_id须与campus_id在同一有效期内的分区归属一致；年段学科按授课身份条件必填；业务身份不充当权限 |
| `role_assignment` | `user_account_id`、三类访问角色、范围、有效期 | 同角色同范围有效期不得重叠 |
| `person_relationship` | 老师、校区/组长/导师关系、有效期、组长关系`effective_week_id`、旧/新对象、管理员及版本 | 仅管理员操作；普通周组长变更重算当周及后续，不改更早周；换校区只联动校长，其他关系及寒暑假中途变更时间粒度待D32 |
| `organization_unit` | `id`、`type`、`name`、`parent_id` | 总部为根、分区隶属总部、校区隶属一个分区；教研组另保留跨校区关联，不充当财务层级 |
| `campus`、`campus_region_assignment` | `campus_id`、`region_id`、`valid_from`、`valid_to`、版本 | `campus.region_id`为当前归属外键；每校区同一有效期恰属一个分区，保留历史版本；仅管理员变更，查询匹配历史归属，变更时间粒度待D32 |
| `venue`、`venue_view_grant` | 场地ID、创建老师、唯一场地账户、名称/状态、默认选择、查看老师及授权/撤销版本 | 每venue独立VENUE账户；创建事务原子创建场地/账户/所有权，重试幂等；一人多场地，查看授权只读、不按名称/校区去重 |
| `referral_venue_assignment` | 独立生源记录、授课场地、effective_week_id、关系版本、操作者 | 接收老师选择/修改，从当周起重算完整周累计及后续费用，更早周不变；费用保存场地和VENUE收款快照 |
| `planning_mentor_assignment` | 规划师、学业规划导师、有效关系版本、管理员 | 与teaching_advisor关系分开；仅管理员任命/维护，无关系则介绍人10%；任命生效时间粒度待D36 |
| `settlement_account` | `id`、`owner_type`、`owner_id`、`account_code`、`status` | `owner_type=PERSON|COMPANY|VENUE`；PERSON对person_id、VENUE对venue_id分别唯一；场地资金由venue主体独立持有，所有者仅通过授权操作；职务业务账户仍不以任职人作为资金所有者 |
| `business_identity_assignment` | `source_person_id`、`identity_kind`、`payee_account_id`、有效期 | 承载业务分润类别及收款目标，新增推荐身份快照，现行管理费与Q合并为CONSULTATION_CAMPUS_FEE且收款校区待D33，不授予登录权限 |

归属和分润身份保存有效期版本；普通周的组长与授课场地关系按周生效，历史结算保留所采用的关系和收款目标。管理员更换组长后，从生效当周起重算该周及后续已录入费用，不重算更早周；每周累计费用不在周内按天拆分。校区、导师等变更保存前后关系，其生效粒度及寒暑假中途变更归D32，换校区只联动校长、不改变组长和导师；场地按接收记录选择及费用快照独立处理，不随校区变更。组长关系重算仅调整该职责对应原/新收款关系，不以此重算更早周；场地修改按当周及后续费用原/新场地账户差额重算，不改更早周；规则性当前财年重算属于另一类事件，退款按单笔有效净分润撤回处理。非零比例必须有合法收款目标，错误提交不产生有效费用；失败只进独立审计。旧Q/C逐笔配置及历史保留；当前合并咨询管理费给校区账户、由授课剩余承担，只收一次；学业规划介绍默认2%、教师介绍0，收款校区待D33。

### 个人账户归集与独立业务账户

REQ-021关闭D24：个人全部收入进入本人唯一PERSON账户，按identity_kind保留来源明细；职务/校区/联盟业务资金独立记COMPANY账户，不因当前负责人相同而并入其个人余额。奖金由获授权职务账户扣款、老师个人账户加款，同一事务锁两侧账户，权限交接不整体转移账户资金，组长生效当周差额重算仍按F02执行。个人提现锁本人唯一PERSON账户；场地提现锁本人拥有的指定VENUE账户，不合并两类余额，余额由其全部有效ledger计算，不按页面可见分类过滤；工资明细仍隐藏但扣减计入实际余额。校区看板分列F营收与合并咨询管理费，F统计不进入余额；校长的个人业务所得仍只归其个人账户，不自动从校区抽成。

本人收入汇总响应先限制当前用户/角色/主体的数据范围，再按管理员分配的业务身份筛选，最后精确聚合并过滤amount==0的项目；amount<0仍显示。响应区分account_balance与visible_income_total，两者不能相互替代。角色撤销不删除旧身份分配或ledger；授权管理员审计及全量Excel继续包含历史/零值。管理报表在当前操作者授权范围内按被查询主体的身份汇总，不要求管理者自己拥有对应收款身份。任何API按ID查明细也要做相同范围校验，不能凭统一余额获取所有角色明细。

### 5.3 表2：生源记录与周课时费分离

原需求把学生、生源关系和每周费用放在一行。实现必须拆开，但不得建立跨老师、跨课程共享的全局学生主档：

- `teacher_student_record`：`id`、`owner_teacher_id`、`course_context_id`、`display_name`、必要敏感字段和版本。每个老师/课程下都是独立业务文档；同名或现实中同一个人也不建立全局唯一键、不自动关联或去重。检索姓名只用于当前授权范围内展示，不能返回“同一学生”推断。
- `referral_case`：`teacher_student_record_id`、`receiver_id`、`referrer_id`、当前 `class_type`、`location`、`referrer_campus_snapshot_id`（表2“输出方校区”）、`business_status`、`acceptance_status`、`submitted_at`、`unaccepted_expires_at`、`archived_at`、`archive_note`、可空 `copied_from_referral_id`。复制来源只用于审计，不共享学生身份、状态、字段或费用。接收方校区另存`receiver_campus_id`及有效关系快照，不能与输出方校区混用；管理费选哪个校区仍待D33。
- `referral_case_event`：追加保存推送、接收、浇灌、完结、21日未接收归档、再次推送激活和复制推送等原始事件，含操作人、时间、原因和请求 ID。业务不提供“拒收”动作。
- `weekly_fee_entry`：`referral_case_id`、`fee_item_id`、`academic_period_id`、`teaching_week_id`、`settlement_month`、`gross_amount`、`class_type_snapshot`、`referrer_business_identity_snapshot`、`receiver_campus_id_snapshot`、`venue_id_snapshot`、`venue_assignment_version`、`venue_payee_account_snapshot`、`planning_mentor_relation_snapshot`、`campus_consultation_rate_snapshot`（现行合并费率）、`legacy_consultation_rate_input`（历史Q原输入）、`collection_account_id_snapshot`、`referrer_campus_id_snapshot`、`source_case_version`、`version`。同一独立 `referral_case_id + teaching_week_id` 最多一笔有效周费用；改错更新同一稳定 `fee_item_id` 并追加版本/事件，不新建第二笔。逐笔输入和源版本在录入时冻结，不能因生源记录以后修改而污染历史计算。只有通过全部必填、比例和收款人校验的请求才能产生新版本。
- `weekly_fee_event`：追加保存创建、确认、更正、作废及更正原因。

REQ-022场地必填校验不依赖生源接收状态：未接收记录仍允许录费，但凡适用场地比例非0，必须解析有效实际venue及VENUE收款账户；本人默认也须真实存在且具备有效选用资格。缺场地、无效账户或无选用资格时整笔拒绝，不更新费用/净额/分配或形成半笔账，不回退原表1场地人、不虚构默认。选用资格的目录范围按D35确认后实施。

收费账号留空时默认接收方。收费账号不等于接收方时，沿用原文“线下处理、系统仅记录”：仍记录各方应收分配，不自动向任何账号划款，也不把整笔 F 当作收费账号的绩效收入。

接收老师按管理员建立的学年计划和 period 选择周次录入。没有财务确认步骤；提交前按候选输入试算全部受影响月份的档位和分配，所有受影响行满足必填、引用和比例规则后才接受写入并进入实时计算；任一受影响行失败则整次提交拒绝，保持原费用版本、净额及余额，不按生源“是否已接收”状态额外过滤。非零比例缺少收款人、总比例超过100%或其他提交错误时返回422及逐字段错误，不创建/更新业务记录；编辑失败保留原成功版本继续计算。合并咨询管理费沿用比例精度合同以百分数校验0～100、最多两位小数，再精确转换为内部0～1比例；不隐式舍入超精度输入。周课时费允许显式0，合法gross_amount为非负、最多两位小数；null/空白不转换成0。0金额行仍保存原始记录、版本与零分配结果，纳入导出；不创建虚假收入。合并咨询管理费及其他非零比例收款人验证照常执行，不能按金额为0跳过。退款按REQ-020生成关联原实际分配的唯一反向流水，不触发整月重定档，不录无来源的负F流水。

输出老师把已填写内容从推给 A 的记录复制再推给 B 时，服务端创建新的 `teacher_student_record` 和新的 `referral_case`，复制允许的资料字段并记录 `copied_from_referral_id`；B 的字段、状态、费用和生命周期完全独立，已发生费用永不复制。新接收方不能复用旧接收方的场地选择授权或默认，必须按新接收方资格重新选择实际场地或解析其本人默认场地。输出老师的查询视图按 `referrer_id` 聚合其推给 A/B 的全部独立记录及各自费用。复制到新接收老师与对原推荐记录的再激活是两个不同命令，不能复用同一状态转换。

未接收记录从输出老师成功提交登记的 `submitted_at` 起严格计算21日，由受控任务按原未接收条件归档并要求备注。归档只隐藏记录并禁止新增周费用，归档前已有费用继续参加计算；向同一目标再次推送产生 `REACTIVATED` 事件并恢复原 `referral_case` 及 `teacher_student_record` ID，同时以激活提交时间重新设置21日窗口，不复制或重复生成原有费用。

### 5.4 学年计划、特殊周与录入截止

- `academic_year_plan`：系统管理员提前设置学年、计划版本和开始/结束边界；全部业务时间使用 `Asia/Shanghai`；生源超期、录入截止和财年窗口均由服务端时钟判断。
- `academic_period`：学期/阶段及其有效日期范围。
- `teaching_week`：所属 period、序号、`week_kind=REGULAR|WINTER_SPECIAL|SUMMER_SPECIAL`、起止日期和锁定的 `settlement_month`。每个寒假、暑假各有一个特殊周。
- `entry_deadline_rule`：按字段/业务阶段保存默认截止，周二 24:00 精确解释为北京时间周三 00:00；`deadline_override` 保存系统管理员延长后的周次、字段、新截止、原因、操作人和版本。

周流水只引用已发布计划，结算月份随周计划锁定，客户端不能另传任意月份。超期后普通账号不能写入；系统管理员必须先创建可审计的 deadline override，老师或获授权人员才能在新截止前补录，补录仍归原期。旧财年错误不由老师直接操作：老师口头告知财务后，获授权财务可创建 `HISTORICAL_FISCAL_CORRECTION`，补原记录并记录原教师说明、原期、理由、操作者及证据引用；服务端按原期所属财年规则与历史关系重算。原期间已经截止时仍须系统管理员先延长截止，该专项业务权不自动绕过期间锁定。服务端使用北京时间可信时钟判定，不依赖设备本地时钟。

### 5.5 表8及费率版本

REQ-022分配已明确：学业规划介绍池默认10%，有规划导师时内部拆为规划师8%/导师2%，无导师关系仍10%；学业规划导师本人介绍10%不自抽，规划招生咨询管理费沿用2%；教师介绍12%、咨询管理费0。Q与原C是同一项，只收一次，从授课老师剩余份额承担并给校区账户，不能额外给学业规划本人。采用唯一类别`CONSULTATION_CAMPUS_FEE`及单一有效费率，旧字段仅作历史兼容，禁止客户端同时提交独立Q/C导致重复计算；收款为介绍方还是接收方校区仍待D33。旧班型9%/12%不再重复作为介绍费基础。`ALLIANCE_DYNAMIC`独立归联盟业务账户，正向按接收方当月净额对应动态比例×F计算；负向及原16%封顶适配待D30，不擅自归零或使用负份额。当前十项金额守恒，受D30/D33影响的分支未决前不宣称全量可执行。

`rate_policy_version` 保存政策版本、所属财年、适用起点、基础费率、上限、状态和创建依据；`rate_tier` 保存上下界、包含方向、抬升比例和是否执行。新财年建立时可从上一有效规则复制为该财年的起始版本，但随后底层结算规则修改只作用当前财年；旧财年账簿和规则版本不接入新财年变更。`CONFIGURE_RATE`不可单独下放，只能在SYSTEM_ADMIN当前职责下使用；所有比例配置的写入均同时校验当前管理员身份和能力，费用、人员安排等接口不得接受或隐式改变比例字段。四职务的配置读取改用本人当前职责份额投影，不直接返回`split_policy_rate`完整对象。

当前初值均由管理员维护：规划介绍池10%（有导师时8%/2%，无导师10%，导师自荐10%）/校区咨询管理2%、教师介绍12%/校区咨询管理0%，组长6%、授课指导导师7%、场地5%、平台财务2%、分区财务1%；联盟动态使用既有净额档位正向调整幅度，负档和上限待D30。咨询管理费默认2%已确定，D27关闭，不再另设独立Q初值。授课老师是其他九项后的残值，不新增独立固定比例；旧班型基础、旧Q/C、旧16%及全部配置按政策版本保留历史，不叠加到现行算法。

`split_policy_version`以接收方为对象，保存单一所属财年、默认/个别来源及操作人；老师个别配置优先，系统默认修改只重算当前财年采用该默认的流水，不覆盖个别配置。咨询管理费使用单一配置，教师介绍不适用时为0，不能沿用旧Q在旁边另收。历史逐笔Q原输入、来源版本和有效结果保留；现行记录冻结合并项目实际采用值与政策来源，不因旧字段映射生成新账。规则性修改保存旧版本，只重算当前财年适用费用，不传播旧财年。

费率及表8分配规则版本发布前生成当前财年 impact preview（现行统一咨询管理费按适用政策处理，历史Q/C不因兼容映射重算）：受影响月份、人员、流水数和预计 delta，并用候选规则重新验证该财年全部已有费用的比例与收款人。若任何已有费用会因候选规则缺收款人或比例错误而无效，整个规则发布事务返回 `RULE_PUBLICATION_WOULD_INVALIDATE_FEES` 和待补齐清单，不创建规则版本、不启动重算、不部分生效。需要重算的规则发布成功后创建当前财年 `recalculation_batch`，旧计算版本永久完整保留，新版本以输入哈希、规则版本和来源批次关联。

### 5.6 表3、表7及不可变结算明细

计算与结算采用以下模型：

- `fiscal_year`：财年代码、开始/结束边界、业务时区、状态。财年固定为北京时间 `[当年9月1日 00:00, 次年9月1日 00:00)`，即业务展示至次年8月31日结束。
- `settlement_period`：所属财年、结算月份、当前发布计算版本、版本和更新时间。实时结算没有“财务确认后才计算”的门槛。
- `calculation_run`：月份、接收方、输入快照哈希、算法/规则版本、触发原因、状态 `RUNNING | SUCCEEDED | FAILED | SUPERSEDED`、错误。费用创建或改错同时影响接收方加项和输出方减项，按有效政策重算二者受影响月份；费用纠错涉及接收方/输出方或月份变化时覆盖原对象及原月份。组长/授课场地变更只处理当周及后续关系；其他关系生效粒度待D32；退款独立原笔冲回，后续动态净额衔接待D34。规则性配置变更通过batch只重算当前财年。
- `allocation_line`：关联run和周费用，保存F、推荐人业务身份、介绍池比例及介绍人净额/规划导师子份额、规划导师关系快照、场地/账户快照、联盟动态比例/金额、净额与档位、单一咨询管理费及其他绩效比例、历史Q/C原字段、原实际收款对象/关系版本和精确金额。旧班型基础费率仅存历史解释字段，不能再次加入介绍费。
- `ledger_entry`：不可变 delta 明细，含原业务月份、发布时间、账户、分润类别、方向、金额、来源 run、前一发布版本和幂等键。内部划拨以同一 `transfer_id` 成对记录发起账户负向、目标账户正向两个 entry；重算只写新旧计算版本的差额，不能把整月再次入账。
- `monthly_statement`：按结算账户 + 月份 + 分润类别的可重建投影；同一人多身份分列，账户余额再累加。
- `fiscal_year_book`：按账户+财年保存独立期内 ledger 投影和规则版本边界，旧财年账目不与新财年明细混算。已确认分年统计、实际余额跨年保留。新年期初承接上年期末，累计余额仍由不可变ledger累计，禁止清零或再生成一笔相同收入以免重复计入。
- `account_balance_projection`：从已发布 ledger 构造账户余额及各财年独立报表。老师只返回当前财年允许展示的本人明细，响应不包含工资现金/扣豆/关联冲回明细；本人实际余额仍含全部有效工资扣减；venue_balance按指定VENUE独立计算，不进入account_balance个人值，受邀看板不得返回所有者个人余额。总部财务及系统管理员按当前视角可查完整账务；其他管理层仅查询获准数据种类及历史范围，分区财务视角不能调用其他老师个人余额或工资投影，本人个人业务须按教师视角原权限访问。旧财年更正回到所属 `fiscal_year_book`，使用该财年的规则版本和历史关系，不能套用当前财年新规则；差额立即进入当前累计欢乐豆余额，但旧期明细仍标为原财年收入，不能混入当前财年收入。

REQ-020营收口径已明确：校区总营收=该校区各老师的分配前课时费F之和，已退款原笔从有效营收扣除且退款独立列示；原费用行保留，校区管理费另列，覆盖REQ-018把校区分润管理费当成校区营收的口径，D31关闭。分区总营收汇总各校区同期间F总额，自身分润及管理费独立展示，不将两者再加进总营收或负责人个人余额。

`region_revenue_view`按同一期间、分配前F及一致的已发布版本集合、历史有效归属读取各校区有效营收并求和。统计为查询投影，不生成allocation或ledger；迁区保留原有效期的历史归属，时间粒度依D32确定。费用统计替代课时统计，不从累计费用推算课时数。退款金额和状态保留独立维度，并从有效F营收扣除，退款操作/部分处理尚待D34，不能用旧退款整月重算更新其他费用。

内部流程：

```text
完整周流水 -> 整月计算 run -> 校验成功 -> 原子发布 active run
           -> 新旧 run 差额 ledger -> 月度分类汇总/财年独立账簿
原期费用改错 -> 归属原月份的新输入版本 -> 按有效规则重算 -> delta 发布
退款 -> 退款前最新有效净分配快照 -> 唯一反向流水 -> 原表单已退款（不重算其他笔）
更换组长 -> 生效当周及后续关系重算 -> 旧/新收款差额（更早周不变）
规则变更 -> 当前财年 impact/有效性预检 -> 分月重算 -> 原子批次发布
```

发布过程在事务中锁定受影响月份/财年，确认输入哈希未变化，写完全部 delta、审计和新 active 指针后一起提交。失败时旧版本保留且界面标明本次计算失败/待更新，不得将旧余额冒充最新；成功后各端只读新active版本。批次重试以 `previous_run_id + new_run_id + account + category` 唯一，避免重复余额入账。

补差记录必须带 `origin_period_id` 并回到原期参与整月重算；页面同时显示原业务期和本次发布时间。`HISTORICAL_FISCAL_CORRECTION` 同样必须带原期、旧财年规则和历史关系快照，且只允许授权财务创建；其差额立即更新累计余额投影，旧财年明细不改写为当前收入。历史计算与旧规则证据永久完整保留，不能因实时结果更新而覆盖。

REQ-020退款通过`fee_refund`关联原费用、退款前最新有效已发布分配/完整入账净额链、原因、操作者和请求ID；表单标记已退款，同一原业务只生成唯一反向流水组，逐项撤回该笔退款前实际仍归属各账户的净金额。“原实际分配”指这份有效净分配，不是机械采用最初版本。冲回使用原实际金额与对象，不重新套当前比例，不因退款重定整月档位或重分其他费用。原记录、原分配和退款反向流水永久保留，已发生提现不撤销，余额可为负。退款与原费用的并发改错/重算须串行化，已冲回分配不得被后续计算重新入账。退款登记操作人、是否支持部分退款及部分冲回算法尚未明确，不提前给普通老师/财务开放退款动作，也不把部分退款按比例折算当成已确认。退款对后续新费用联盟动态净额口径的影响随D34明确，不能恢复旧整月退款重算路径，也不能据此冻结正常费用改错及后续动态计算。原组长A记60，同周换组长已发布A−60/B+60，再退款应冲回B60；若以后换岗未影响原周，则仍冲回原周实际收款的A。场地当周重算后退款同样使用最新有效VENUE净分配，不能再扣最初场地；已提现导致旧场地负余额仍保留，不能撤销真实提款历史。退款不得再通过普通付款单据重复扣减。

表7最终结算余额（本次按用户所称“欢乐豆余额”记录）使用可带符号的精确十进制值，允许小于零；数据库、API 和客户端不得施加余额非负约束或使用 `max(balance, 0)`。负余额在看板和导出中保留，不能当成单笔分配超额错误。提现独立校验足额正数，提交即扣其PERSON或指定VENUE来源账户豆，财务外部转账标完成不二扣；余额允许为负不意味着可以超额提现。余额由全部有效 signed ledger 累加：工资现金支付确认在总部财务/管理员账务中以老师个人负向ledger分列，奖金、报销及其他已确认内部划拨以双侧方向分列；每个变化立即体现。撤销已入账的业务划拨只追加同额、反向、关联原 `transfer_id` 的唯一成对冲回，不覆盖期间其他变化，账户可为负数；已转账提现不属于此可撤销范围。已提交的正向人工调账已经入账，可以用于提现。

### 5.7 表4：有效性、审批与入账分离

REQ-020报销自审例外：财务老师须先在个人视角提交本人报销，再切换获授权总部财务视角审批该单，可以自己审批；系统记录两次当前视角及同一自然人身份。管理员可查看，管理老师具体查看范围待D34，不能因此开放全公司财务。该例外仅适用本人报销，不覆盖人工调账禁止自审规则，也不预设对外付款可自审。

`finance_document` 保存单据类型、自然人申请人、业务目标账户ID、实际支出账户ID（个人提现为本人PERSON、场地提现为本人拥有的VENUE，报销/付款为获授权职务业务账户）、金额、收款资料、发票号及版本；`finance_document_event`追加保存提交、自动通过/审批、拒绝、完成和撤回行为。报销沿用职务支出账户扣款、申请老师个人账户加款，账户归属已按REQ-021确定，不混入执行财务个人钱包。提现单独冻结银行卡账户及姓名快照，金额与收款资料在提交后不能由用户修改。

必须分开三个概念：

- `data_validity`：字段和引用是否满足当前产品规则，值为 `VALID | INVALID | UNRESOLVED`。
- `approval_status`：沿用产品的待审核、已通过、已拒绝、财务已撤回、已完成；提现创建即已通过，完成前财务撤回仍保留此前系统自动通过事件；拒绝状态用于需人工审批的其他单据，不给提现增加人工审批门槛。
- `posting_status`：技术状态 `UNPOSTED | POSTED | REVERSED`，表示是否已产生 ledger。

REQ-021提现规则及REQ-022场地来源：本人提交提现时必须填写银行卡账户与姓名，金额为正且精确到分。服务端按提现来源校验本人PERSON或本人拥有的VENUE账户，锁定该唯一来源账户，在事务内读取其最新可用余额；场地入口固定venue_id与source_account_id，服务端不信任客户端跨场地换账户，不可合并个人或其他场地余额；金额超出余额返回`INSUFFICIENT_AVAILABLE_BALANCE`并显示“超出可用金额”，不创建成功单据、不扣豆。请求冻结来源类型、来源账户和venue_id（场地必填）；余额足够才原子创建已自动通过且`POSTED`的提现单、写唯一负向ledger并更新余额，页面显示待财务转账；金额大小不改变流程。余额可因工资或更正为负，但负余额不能提交正数提现。

提现提交后用户不能修改或撤回；系统不与银行交互。财务在系统外自行转账后标记完成，保存操作人、时间及付款记录，不再次扣豆。完成前获授权财务可手动撤回：锁单据及账户，以原提现ID为唯一返还键向原出款PERSON或VENUE账户追加等额正向ledger并置`REVERSED`；不得删除原单或覆盖其他收支。完成与撤回采用条件状态更新及同一锁串行，只有一项成功；重复提交、完成或撤回均幂等。已完成提现不能自动返豆，纠错另走人工调账并关联原单。银行卡资料属于敏感字段，日志及错误响应不得暴露完整值。

报销/付款保持人工审批流程，不能提现自动通过扩展到其他单据。**报销已批准并由财务执行完成时**，服务端在同一事务锁定发起支出账户和申请老师个人账户，创建同一 `transfer_id` 的“发起账户 − 金额 / 申请老师 + 金额”两条 ledger、更新两侧余额和单据状态；这是内部欢乐豆划拨，与线下支付无关。审批通过本身不自动再付一次；只有内部划拨成功才为“完成”，失败则完整回滚。撤销该业务划拨只能对同一双侧记录作一次同额反向冲回，不能覆盖其间其他收支。对外付款保持人工审批及线下办理，在确认完成时仅减少执行人获授权支出账户的欢乐豆，外部收款人不获得内部账户收入。职务业务支出不影响执行人个人钱包，对外付款不产生内部收款方加豆；系统不调用银行支付接口。

### 5.8 人工调整欢乐豆

人工调账使用独立的不可变 `balance_adjustment`：`id`、`settlement_account_id`、带符号 `delta_amount`、`before_balance_snapshot`、`after_balance_snapshot`、`reason`、`created_by`、`actor_role_assignment_id`、`created_at`、`submitted_at`、`related_document_type/id`、可空 `approval_route_id/approver_id`、`request_id`。原始 delta、before/after、原因、目标账户、操作者和当时职责一经提交不可变。审批或免审动作写入追加式 `balance_adjustment_approval_event`，当前 `approval_state=PENDING|APPROVED|REJECTED|RETURNED|NOT_REQUIRED`、`posting_status=POSTED|REVERSED`、各动作时间和 `version` 是从事件维护的并发投影；状态更新不得修改原始字段。

提交调整时立即改变账面余额，并在一个数据库事务中完成：校验 `CREATE_BALANCE_ADJUSTMENT` 与范围、目标为授权范围内结算账户（通常为PERSON；已完成场地提现纠错须关联原单并指向原VENUE来源账户）、原因非空、delta非零且最多两位小数、当前职责有效 → 按统一顺序锁结算账户 → 读取最新 before 并计算 after → 创建 adjustment → 写一笔类别为 `MANUAL_BALANCE_ADJUSTMENT` 的不可变 signed `ledger_entry` → 更新余额投影 → 财务职责创建管理员审批任务并标记 `PENDING`，`SYSTEM_ADMIN`职责追加免审事件并标记 `NOT_REQUIRED` → 写审计。任一步失败全部回滚。`request_id` 绑定请求载荷；同请求重试返回原结果，换载荷报幂等冲突。账户锁使人工调账与提现、重算、工资现金确认扣豆及其他入账串行，每笔都在事务内读取最新余额，禁止用客户端或陈旧快照覆盖余额。

财务提交的 `PENDING` 调整由管理员审批：通过只追加审批事件并改为 `APPROVED`，`posting_status` 保持 `POSTED`，不得再次写 ledger 或改变余额；财务创建者及同一自然人切换角色后都不能审批自己的记录。管理员/实名授权开发者提交的 `NOT_REQUIRED` 调整不创建审批任务、没有 approve/reject/return 入口，免审事件显式保存职责和授权来源。财务调整被拒绝或退回时，在同一事务中改为终态 `REJECTED` 或 `RETURNED`、把 `posting_status` 改为 `REVERSED`，并追加原 `delta_amount` 相反数的冲回 ledger；冲回 ledger/事件保存当时余额 before/after，唯一 `reverse_key` 取原 adjustment ID，确保每笔只冲回一次。冲回不覆盖期间其他收支；正向调整已提现后仍可冲成负余额，原提现不撤销。退回后修改再提交创建新的关联 adjustment 并重新入账，旧记录永久保留。approve/reject/return通过版本条件和账户锁并发互斥。全部调整、审批和免审日志向系统管理员提供全公司完整查询。

REQ-022完成后场地提现纠错沿用财务送管理员审批、管理员免审及不可变记录规则，只调整原单VENUE来源账户，不返到所有者PERSON；场地所有者或查看者不因持有/查看场地获得CREATE_BALANCE_ADJUSTMENT。调整请求必须同时校验操作者原有财务/管理员能力与目标账户范围。

人工调账属于表7独立余额分类，不参与净课时费档位、周费用、月度分润或财年规则重算。表7在原有结转与分润基础上按账户分列工资现金支付扣豆、奖金、报销及人工调整的已发布 ledger；内部划拨的发起账户与收款账户必须各自保留方向，不能用单边净额冒充双侧变化。审批状态本身不直接参与加总。月度/财年重算只能追加计算差额 ledger，绝不能覆盖或抵消现金工资扣豆、人工调账或已完成内部划拨；更正必须追加可追溯冲销/差额。

### 凭证图片与原件存储

首版支持发票和付款凭证图片。采用 `attachment` / `attachment_version`，关联财务单据，保存不可变对象键、原文件名、MIME类型、字节大小、SHA-256、上传者、时间与 `UPLOADING|READY|FAILED` 状态。服务端验证授权、实际文件格式和大小；首版支持JPEG/PNG原图，预览图可另生成但不得覆盖原件。附件新增或更换保留旧版本并记录审计，单据退回/撤回不删除原件。

原件READY前不声称上传完成。采用临时上传、内容核验、不可变对象落存和数据库READY提交，失败可清理未被引用临时对象，绝不清理成功原件。下载通过单据范围和角色权限检查，私有对象不可裸链公开。永久留存对应原件、历史版本、业务记录和成功备份，短效访问令牌到期不删除文件。

### 5.9 表5、表6：总部财务工资及稳定项目 ID

工资资格、计划、现金实发与扣豆配置/记录仅在总部财务管理页维护，系统管理员可管理。保存`salary_eligibility(person_id, enabled, effective_from, effective_to, operator, version)`，由该配置确定需要生成工资待办的老师；停用不删除历史工资或已支付记录。工资配置保存现金计划额与对应扣豆额，确认时按1元对应1豆核验实发与扣豆一致，不与课时分配参数混用。教师个人页没有工资模块、现金实发或工资扣豆明细；服务端按当前授权上下文过滤所有个人报表、余额明细、单据详情、搜索及增量同步中的工资字段、工资扣豆及其关联冲回，不把完整记录发给客户端后隐藏，也不通过“其他扣费”变相暴露同笔工资明细。个人余额查询继续以全量有效ledger计算，因此扣减后的余额真实显示。兼任总部财务/管理员者在个人教师视角也受该限制，切换管理视角才能查看；账务和全量备份完整保留。

`compensation_item_definition` 保存 `id`、`category`（奖金/扣费）、稳定槽位 1–10、公司统一显示名称和有效期；`compensation_entry` 保存人员、月份、项目 ID、金额、来源和创建时的名称快照。公司统一改名生成新名称版本，既有记录继续展示原快照，改名后新建记录使用新名称；不批量改写历史名称。金额逐人逐月录入。工资单独记录现金支付与个人扣豆，奖金保持内部欢乐豆划拨；两者与课时费分配独立。

基本工资单独使用版本化 `base_salary_schedule(person_id, effective_from_month, amount, source, version)` 作为循环基准，使用 `base_salary_month_override(person_id, month, amount, version)` 表示仅单月覆盖。更新模式必须显式选择：`THIS_MONTH_ONLY` 只改选定月；`FROM_THIS_MONTH` 从选定月发布新基准，并覆盖该月及以后已有基准/单月工资设置，旧版本永久保留，不影响更早月份。发布前生成影响预览，列出已生成月份的预期差额和未来设置；不能让旧 override 优先级悄悄保留旧值。

`base_salary_generation` 按“人员 + 月份”唯一生成待支付工资记录，附生效设置版本；未来月份只保存计划。北京时间（Asia/Shanghai）每月1日00:00生成当月待办，任务重试不重复生成，计划变更更新版本而不另造同月待办。计划与待办均不扣豆或加豆；财务按月统一选择待办，现金实际发给老师后再确认已支付，不执行定时自动扣豆。

`salary_payment` 保存工资月、收款老师、资格配置版本、计划金额、配置扣豆金额、实际现金支付金额、老师个人账户、现金支付时间、财务确认人和时间、理由/说明、原工资及补差关联和唯一支付确认幂等键。现金支付状态与欢乐豆入账/更正状态分列，不能用`REVERSED`等单一状态宣称真实现金已撤销。获授权财务确认已支付时，同一事务锁定老师个人账户，创建类别`SALARY_CASH_DEDUCTION`、金额为实际支付金额负值的唯一ledger，更新本人余额及确认状态。允许个人余额为负；重复确认返回原结果且不重复扣款，同一幂等键换载荷拒绝。没有工资发起人扣豆/老师加豆的双侧`transfer_id`，也不因记录现金付款来源另扣一个出资账户的欢乐豆。现金支付及工资扣豆不参与净课时费、分配比例、课时分润或财年规则重算。

基准或单月设置改变未支付月份时只更新计划；改变已支付历史月不自动补扣或返还、不重写原记录。涨薪生成关联原工资的差额待办，现金补发后财务确认仅扣实际补发差额，不能再次扣全额。已确认工资记录发生录入错误时，追加有理由、关联原扣豆ledger的唯一反向冲回，按需要另建正确支付确认记录；保留真实现金支付历史，账务冲回不表示真实现金已退还。重复冲回不得再次改变余额；后续其他收支不被覆盖。奖金不随工资自动发放；医社保、公积金只扣财务职务账户，不套用工资或个人扣费参数，到期先生成待办，财务确认才扣豆。

奖金由财务单独勾选并填写理由后发放：财务获授权的职务账户减额、目标老师个人账户加额，在同一事务立即生效；撤销同样只产生一次反向双侧冲回。公司统一项目名称属于系统管理员维护的配置；获授权财务以全公司范围录入工资、奖金及扣费项目金额，不因录入权限自动取得配置改名权限；网页提供表格操作，微信小程序和手机网页提供可发现的“更多”入口。工资和奖金均按人 + 月份保存，不把多个自然月覆盖在同一字段。医社保、公积金由财务职务账户承担：执行时只写该账户负向ledger，不另扣老师个人、不产生个人正向收入；按人员/月份保存成本归属与职务支出主体。财务设置每月执行日，版本化保存日期、金额与启停；到期只生成待办，账户余额不变。获授权财务确认待办时锁定职务账户，同事务写唯一负向ledger、更新余额和确认状态。以计划项+人员成本对象+月份作为稳定唯一业务键，改日期、任务重试和重复确认不能生成第二份待办或第二笔扣款；已确认月不因日期改变再次执行。日期配置必须能解析每个适用月的有效执行日期，保存时校验并预览，调度巡检补齐遗漏待办，不擅自限制为1至28日。全部业务时间为Asia/Shanghai，系统不自动办理外部社保现金支付，D26关闭。

医社保/公积金模型：`benefit_schedule`保存稳定计划ID、人员成本对象、项目、承担职务账户、月份范围、执行日期规则及版本；`benefit_month_task`保存计划ID+对象+月份唯一键、当月有效执行日、金额快照和`PENDING|POSTED`状态；`benefit_confirmation`保存当前财务职责、确认人/时间及唯一ledger引用。计划到期、确认与历史更正事件分开保留，不能把待办视为已支付或借计划改动覆盖已入账记录。

### 5.10 原始记录、派生状态和用户视图

| 层次 | 示例 | 修改规则 |
|---|---|---|
| 原始记录/事件 | 生源状态事件、周费用事件、配置版本、审批事件、补差原因 | 追加保存；错误通过作废/冲销事件更正 |
| 派生状态 | 当前生源状态、最新配置、试算 run、月度 statement | 可从事件和不可变明细重建；带版本和来源游标 |
| 用户视图 | 表1–表8、个人看板、校区/分区看板、Excel | 只读组合；按权限和快照时间生成 |

Excel 导出和看板都是数据库在指定快照下的用户视图，不能反向成为数据库真源。D20 已确认本期不提供 Excel 导入入口、接口或后台任务。

## 6. 计算、幂等与并发

### 6.1 计算流程

本节档位计算现在只服务联盟动态项，不能增减规划介绍池10%或教师介绍12%；规划池内部8%/2%不受动态档位影响。原班型基础9%/12%不再加入个人介绍费；动态负档和封顶待D30，收款使用独立联盟业务账户。退款本身不触发整月重定档；后续动态净额衔接待D34。普通周组长和授课场地关系变更以生效周为边界，只更新该周及后续关系，更早周不变；其他关系粒度待D32。

1. 由新增或变更流水确定新旧接收方、输出方及月份的影响集合，按各人月读取已成功提交的接收与转出原始费用；不按接收状态或财务确认过滤。缺收款人或比例错误的请求已在写入边界被拒绝，不存在“保存错误费用后再决定是否计入净额”的正常路径。
2. 按已确认口径计算个人当月净课时费：`本人作为接收方的 F 总额 - 本人作为输出方推荐给其他老师学生的 F 总额`。所有进入集合的费用都已通过提交校验；失败请求不产生费用或净额。
3. 按费用所属财年解析该财年已发布有效的政策版本，检测区间唯一命中。当前财年规则修改只覆盖当前财年；旧财年补录或更正使用所属财年保留的规则版本，不直接使用执行时的新财年规则。
4. 读取单一咨询管理费规则及适用身份、介绍池与规划导师关系及8%/2%子份额、联盟动态项收款目标和表8版本，并在计算前重复断言不变量。若发现历史脏数据或引用损坏，整个计算 run 标记 `FAILED_INVARIANT` 且不发布；这与规则发布前返回待补齐清单是两个不同失败类型。
5. 对按最终推荐费率规则判定的合格行用高精度十进制计算各项精确份额，执行“向下取整到分 + 最大余数逐分分配 + 固定类别顺序决胜”的守恒算法。不同分润身份分别生成 allocation line，即使收款账户相同也不合并类别。
6. 生成整月新 run、输入哈希和与前一 active run 的逐项 delta。
7. 在串行化事务中重新核验输入和前一版本，原子写入 allocation、delta ledger、审计和 active 指针；失败继续展示旧版本并安全重试。

规则性配置修改仍按当前财年范围及版本管理，介绍人固定费率与联盟动态项分列。D30动态负档/上限、D33咨询管理费收款校区未明确前，不发布混合算法。费用改错与退款分开：改错按有效政策重算，退款只撤回原笔实际分配，不整月重定档；组长更换与场地修改只从生效当周处理当周及后续关系，更早周不变。

### 6.2 幂等

所有写 API 要求客户端生成 UUID `requestId`。数据库 `idempotency_record` 以 `actor_id + operation + request_id` 唯一，保存请求摘要、结果状态和响应摘要：

- 相同 requestId + 相同请求重试：返回原结果，`outcome=REPLAYED`。
- 相同 requestId + 不同请求体：返回 `IDEMPOTENCY_CONFLICT`。
- 后台 job 使用业务唯一键，例如自动导出`export:{scheduleId}:{scheduledWindowStart}:{exportType}`（不得把每次重试变化的snapshot作为调度幂等键）、`recalc:{scope}:{inputHash}:{ruleVersion}`。
- delta ledger 使用 `previous_run_id + new_run_id + account_id + category + source_line_key` 唯一，避免重算重复入账。

### 6.3 并发和事务

- 普通编辑用 `version` 乐观锁，版本不一致返回 409 `VERSION_CONFLICT` 和服务器当前版本。
- 生源接收/激活、周费用录入、审批和配置发布在一个事务内写主记录、版本事件和审计。
- 实时月重算发布使用 `SERIALIZABLE` 事务，并对 `settlement_period` 执行行锁；捕获序列化失败后按有限次数、随机退避重试，仍失败则保留旧 active 版本并报告。
- 财年规则重算先完成所有候选月份和 delta 校验，再以 batch 状态和版本指针原子发布；任一月份失败则整批不切换。并发的新流水使输入哈希变化时，废弃候选并基于新输入重算。
- Excel 任务领取使用 `FOR UPDATE SKIP LOCKED`，失败记录错误和重试次数，超过阈值进入人工处理，不丢失 job。
- 所有外部文件写入采用“临时对象 -> 校验和 -> 原子发布元数据”；数据库事务不假装与对象存储形成分布式事务。

## 7. API 契约

API 使用 `/api/v1` REST + JSON，并由 OpenAPI 文件作为客户端和服务端的共同契约。列表统一使用游标分页；金额和费率在 JSON 中使用十进制字符串，防止客户端浮点误差。

### 7.1 写请求与响应

```json
{
  "requestId": "uuid",
  "version": 3,
  "data": {
    "...": "业务字段"
  }
}
```

```json
{
  "requestId": "uuid",
  "outcome": "SUCCEEDED",
  "data": {
    "id": "uuid",
    "version": 4,
    "updatedAt": "2026-09-16T12:00:00Z"
  },
  "errors": []
}
```

`outcome` 统一为 `SUCCEEDED | REPLAYED | REJECTED`。错误结构如下：

```json
{
  "code": "VERSION_CONFLICT",
  "message": "记录已被其他操作更新，请刷新后重试",
  "field": null,
  "retryable": false,
  "details": {
    "currentVersion": 4
  }
}
```

财务及结算接口至少定义稳定错误码：`MISSING_PAYEE`、`RATE_DEFAULT_NOT_CONFIGURED`、`SALARY_CASH_CONFIRMATION_ALREADY_POSTED`、`SALARY_DEDUCTION_ALREADY_REVERSED`、`INVALID_SPLIT_RATIO`、`RULE_PUBLICATION_WOULD_INVALIDATE_FEES`、`CALCULATION_INVARIANT_FAILED`、`INTERNAL_TRANSFER_ALREADY_EXECUTED`、`INTERNAL_TRANSFER_ALREADY_REVERSED`、`BALANCE_ADJUSTMENT_SELF_APPROVAL`、`BALANCE_ADJUSTMENT_APPROVAL_NOT_REQUIRED`、`BALANCE_ADJUSTMENT_FINALIZED`、`HISTORICAL_FISCAL_CORRECTION_FORBIDDEN` 和 `IDEMPOTENCY_CONFLICT`。提现另定义`INVALID_WITHDRAWAL_AMOUNT`、`INSUFFICIENT_AVAILABLE_BALANCE`、`WITHDRAWAL_USER_EDIT_FORBIDDEN`、`WITHDRAWAL_ALREADY_COMPLETED`与`WITHDRAWAL_ALREADY_REVERSED`；超额错误固定显示“超出可用金额”。费用创建/更新校验失败返回422且不改变原业务版本；规则发布预检失败与后台计算不变量失败使用不同错误/任务状态。三端只按稳定错误码处理。

`message` 可本地化，客户端逻辑只依赖稳定 `code`。主要状态码：400 字段校验，401 未登录，403 无权限/超范围，404 不存在，409 版本或幂等冲突，422 业务规则未满足，429 限流，500 未预期错误。500 响应不暴露堆栈、SQL 或敏感字段。

### 7.2 主要资源

| 路径 | 用途 |
|---|---|
| `/auth/login`、`/auth/refresh`、`/auth/select-role` | 登录、刷新、角色切换 |
| `/people`、`/accounts`、`/organizations`、`/access-roles` | 个人/公司业务/场地账户、三类权限和范围配置 |
| `/business-identities`、`/settlement-accounts` | 现行十类分润及旧Q/C历史类别、个人/校区公司账户；联盟动态使用独立业务账户，任职人无自动个人收款权 |
| `/academic-years`、`/periods`、`/teaching-weeks`、`/deadline-overrides` | 学年计划、特殊周、结算月和截止调整 |
| `/referrals`、`/referrals/{id}/events`、`/referrals/{id}/copy-to-receiver`、`/referrals/{id}/reactivate` | 独立生源推送、接收、21日归档、复制到新接收方、原目标同ID再激活与完结；复制命令不复制费用 |
| `/auth/register`、`/admin/role-assignments` | 普通注册仅基础本人权限；管理员授予/撤销特殊角色、职责与范围并留审计，普通用户不可写授权表 |
| `/weekly-fees`、`/weekly-fees/{id}/revisions` | 周课时费创建/修订；缺收款人或比例错误整请求422且不生成新版本 |
| `/rate-policies`、`/split-policies`、`/{id}/impact-preview` | 全量配置及修改/复制/批量/发布仅当前SYSTEM_ADMIN；发布前验证全部既有费用，失败整次拒绝并返回管理员可见清单 |
| `/duties/current/own-allocation-rates` | 四职务仅返回本人当前有效职责对应份额及必要版本，不包含团队/其他岗位比例；校长返回其校区职责份额，保持校区账务归属；其他未提岗位不自动获得本接口范围 |
| `/calculations/{month}`、`/recalculations`、`/recalculations/{id}/impact` | 实时月计算、财年重算和影响预览 |
| `/adjustments`、`/fee-refunds` | 原期费用改错与退款分开；退款按原实际分配唯一冲回并标已退款，不重算其他笔；退款操作人/部分退款未明确前不开放对应写入权限 |
| `/finance-documents`、`/{id}/approve`、`/{id}/complete`、`/{id}/finance-cancel`、`/{id}/reject` | 本人报销可个人提交后切财务自审，审批事件记录当前视角；报销批准后由财务complete原子执行内部双侧划拨；重复 complete 不得二次入账，撤销只作唯一反向双侧冲回。提现足额正数提交即通过并扣豆；财务完成不二扣，完成前finance-cancel唯一返豆；完成后纠错另走调账，用户无改撤入口。 |
| `/balance-adjustments`、`/{id}`、`/{id}/approve`、`/{id}/reject`、`/{id}/return`、`/balance-adjustment-approval-tasks` | 财务职责提交即入账并创建管理员审批；SYSTEM_ADMIN职责提交即入账且标记NOT_REQUIRED、不创建任务；财务调整拒绝/退回只冲回一次 |
| `/compensation-items`、`/compensation-entries`、`/salary-eligibility`、`/base-salary-schedules`、`/base-salary-impact-preview`、`/salary-payments/{id}/confirm-cash-paid`、`/salary-payment-differences` | 表5、表6；工资资格和配置/记录仅当前总部财务或SYSTEM_ADMIN视角可访问，教师或REGION视角请求拒绝；工资每月1日00:00生成待支付待办，现金支付后财务确认仅扣老师个人欢乐豆；已支付月改计划产生差额待办，现金补发确认只扣差额。医社保/公积金仅扣财务职务账户，月执行日由财务设置，到期只生成待办、财务确认后扣豆；计划/人员对象/月唯一，改日期与重试不重复。 |
| `/benefit-schedules`、`/benefit-month-tasks`、`/{id}/confirm` | 财务设置医社保/公积金月执行日；到期生成待办，确认仅扣获授权职务账户，计划/对象/月唯一且确认幂等，个人不加扣豆 |
| `/reports/table1`…`/reports/table8`、`/reports/me/balance` | 按当前职责过滤的报表；教师视角不返回工资及关联扣豆/冲回明细，余额包含实际扣减；总部财务/管理员视角才可查看工资完整账务 |
| `/reports/regions/{id}/campus-revenue`、`/reports/regions/{id}/own-revenue` | REGION读取自身分润、各校区分配前F营收及合计，管理费另列，返回期间/版本/历史归属依据；统计不重复入账 |
| `/venues`、`/venues/{id}`、`/venues/{id}/view-grants` | 老师创建独立场地看板；所有者设置只读查看老师，查看者不可提现/再授权/修改比例，服务端校验venue范围 |
| `/referrals/{id}/venue` | 接收老师选择、默认或修改实际授课场地；费用冻结VENUE目标，修改从当周完整累计及后续重算差额，更早周不变，目录范围D35 |
| `/venues/{id}/withdrawals` | 仅所有者对该VENUE账户发起足额提现，前后端固定来源，查看者拒绝；沿用提交扣豆、线下完成、财务撤回原账户一次返豆 |
| `/admin/planning-mentor-assignments` | 管理员任命学业规划导师及维护旗下规划师关系，与授课指导导师关系分开 |
| `/reports/managed-teachers`、`/reports/managed-students` | 校长/组长/授课指导导师按所属人员范围读学生名单、逐笔费用、余额及非工资收入；工资与比例字段限制独立执行 |
| `/regions/{id}/members` | REGION只读本区名单及最小资料，不能修改人员关系 |
| `/admin/person-relationships` | SYSTEM_ADMIN变更校区/组长/导师，换校区仅联动校长；普通周组长变更预览并重算当周及后续关系，不重算更早周；其他关系及寒暑假中途变更时间粒度待D32 |
| `/exports`、`/exports/{id}`、`/exports/{id}/download` | Excel 导出任务、状态和限时下载 |
| `/sync/changes?cursor=...` | 跨端增量刷新 |

具体字段由 OpenAPI schema 定义；所有管理写接口都记录 before/after 摘要和操作者。敏感字段响应显式分为 `FullSensitiveView`、`MaskedSensitiveView` 和 `MinimalBusinessView`，分别绑定已确认的职责与范围，不能先全量返回再由前端隐藏。

## 8. 三端刷新、失败恢复与离线边界

首版采用“服务端真值 + 前台缓存”：

- 每次写成功返回新 `version` 和 `updatedAt`，客户端立即用服务端结果替换本地缓存。
- 缓存键包含登录用户、当前角色/职责、授权上下文版本及查询期间；角色切换时清空旧页面数据和敏感缓存，取消旧请求，丢弃旧上下文迟到响应，避免跨角色闪现。角色撤销触发服务端拒绝旧请求并刷新授权。
- 页面进入、微信小程序 `onShow`、浏览器重新聚焦和网络恢复时调用增量同步；游标失效则重新拉取当前范围。
- 看板前台可短间隔轮询变化游标；首版不依赖 WebSocket。结算和导出等长任务按 job ID 查询状态。
- 草稿表单可本地暂存，但提交必须重新做服务端权限、版本和业务校验。财务写操作不支持离线自动排队，避免恢复网络时重复入账。
- 客户端超时后使用同一 `requestId` 查询或重试，不能生成新 ID 猜测第一次是否成功。
- 401 时只允许一次刷新会话重试；版本冲突显示服务器当前值和用户草稿，要求用户明确重新提交。

## 9. Excel 业务备份与数据库灾备边界

完整字段、工作簿、权限、脱敏、命名和验收契约见 [docs/EXCEL_BACKUP_SPEC.md](docs/EXCEL_BACKUP_SPEC.md)。本文只定义系统实现边界。

### 9.1 一致性快照与任务

完整备份每周五北京时间10:00自动生成，同时只为系统管理员保留手动生成入口。`backup_schedule`保存Asia/Shanghai、周五10:00、启停版本和下次计划；成功备份文件永久保留。调度幂等键按计划周唯一，漏跑由巡检补建同一计划周任务，不重复生成多份“本周自动备份”。

1. 调度器或系统管理员创建导出任务，API 记录触发来源、请求人/系统主体、授权范围和导出类型；此时不把不同时间的查询结果当成快照。
2. worker 领取任务后启动只读 `REPEATABLE READ` 快照事务，记录 `as_of`、数据库快照标识、筛选条件、schema/generator 版本和数据水位。单连接用服务端游标流式读取；若压测证明需要多连接，则由保持打开的协调事务导出 snapshot，所有读取连接在查询前导入同一 snapshot。
3. worker 从同一逻辑快照读取表1–表8、唯一个人账户、独立职务/校区/联盟业务账户及授权历史、VENUE账户/所有权/查看授权/费用场地变更、规划导师任命及介绍池子份额、三类访问角色及逐人兼任授权、现行十类分润及旧Q/C历史类别、独立学生/生源记录及复制来源、学年周计划、工资循环基准/单月覆盖/待支付待办/现金支付确认及单侧扣豆ledger/关联冲回/差异待办、奖金与报销内部划拨及唯一反向冲回、原始流水、全部计算版本、人工余额调整及审批或免审事件、signed delta ledger、审计清单及附件READY版本引用。大数据量分批和跨工作簿拆分仍须保持相同快照，不能让各 sheet 看到不同时间点。
4. 生成 Excel 临时文件，执行 sheet/分片、行数、稳定排序、引用、金额核对、长文本重组和业务文本字符串类型与公式注入检查，计算每个文件的 SHA-256 和数据集逻辑摘要。
5. 按快照中不可变附件版本取原件，核对字节SHA-256，连同XLSX形成文件组或ZIP；生成`package-manifest.json`列出所有表格分片和附件版本的大小、哈希、逻辑摘要和完成状态。READY原件缺失或哈希错误表示备份不完整，不发布成功组。上传中的附件记录原样导出并明确非READY，不冒充已备份原件。文件组写入私有存储后原子发布下载元数据。
6. `export_job` 记录请求人、权限范围、快照时间、schema/generator 版本、行数、各表金额总计、校验和、业务异常状态、任务状态、错误和文件到期时间；`audit_event` 记录创建、失败、下载、撤销与清理。

源数据存在历史业务异常、配置缺失或待决定口径时，完整备份仍须原样导出，并在 manifest/异常清单中逐项标注，不能因为业务数据“不好看”而拒绝备份。只有无法取得一致快照、授权失败、复制不完整、文件/清单生成失败或校验和不一致，任务才标记失败。失败不得留下“成功”文件；重试沿用同一 job 或产生显式 attempt。Excel 中的密码哈希、会话、内部密钥永不导出；只有系统管理员可生成和下载完整导出，导出的手机号、银行卡、开户行、户名、发票及凭证按原始值保真，下载全程受控和审计。首版已确认配套附件原件归档，成功备份和历史业务数据永久保留，manifest必须完整列明。按约300元/月预算核算容量和增长，不因达到配额而擅自删除历史；资源条件不足应明确报告。

### 9.2 与数据库灾备的分工

- **Excel 业务备份**：用于管理员下载、人工核查、移交和业务数据留存；不包含密码哈希、会话、令牌、密钥、数据库角色、索引物理状态或基础设施配置。本期没有 Excel 导入能力。
- **系统灾难恢复**：数据库 schema、约束、审计、幂等记录、认证数据和全部系统关系使用加密 `pg_dump`/物理备份与对象存储备份恢复。Excel 不能替代它。

数据库灾备属于生产部署的基础安全前置，需要用合成数据在隔离环境验收，并在上线前明确 RPO、RTO、保留期、密钥托管和恢复责任人；它不是本轮新增的 Excel 功能，也不代表已经授权购买存储、部署服务或执行真实生产恢复。

## 10. 审计、安全与隐私

- `audit_event` 追加保存操作者、当前角色、数据范围、动作、资源 ID、请求 ID、结果、时间、IP/设备摘要和变更字段摘要；财务记录不得硬删除。
- 敏感字段日志默认遮蔽；银行卡只显示尾号，完整值使用应用层信封加密，密钥与数据库分离。
- 所有线上通信使用 HTTPS；生产密钥通过专用秘密管理注入，不进入 Git、镜像或 Excel。
- 管理端导出、全局查询、费率发布、结算和调整属于高风险动作，要求重新校验会话并留下不可变审计。
- 历史业务数据、凭证原件和成功备份永久保留；逻辑停用与访问收回不删除历史，不设置年度自动删除规则。认证会话到期与失败临时文件清理是独立技术生命周期，不改变业务永久留存。

## 11. 迁移、回滚与失败处理

### 11.1 数据初始化与范围

D20 已确定本阶段不提供 Excel 导入，也不安排历史表格批量接入。开发及验证使用隔离环境中的合成种子数据，正式业务记录通过已授权的业务操作产生。本节的数据库迁移仅指应用升级时的结构演进和内部数据回填，不是面向用户的数据导入功能。

### 11.2 Schema 与应用回滚

数据库迁移采用 expand/migrate/contract：先增加兼容结构，再回填和双读核验，最后在后续版本移除旧结构。应用代码可以回滚到兼容版本；已经入账的数据不能靠 Git 或 schema 回滚撤销，必须用业务冲销/调整。

若早期原型曾建立全局 `student`，迁移时按每个老师/课程/推荐记录复制为独立 `teacher_student_record`，保留旧ID到新ID映射和来源审计；不得按姓名、手机号或推断出的现实身份合并。回滚只切换兼容读路径，不把已拆开的记录重新合并。

迁移失败时事务回滚；长回填任务保存游标并可重复执行。发布后发现计算错误时立即停止受影响月份结算，保留原始输入，修复算法后生成新 run 和差异报告，已结算数据走冲销调整。

## 12. 测试与交付门禁

### Gate A：产品口径可执行

- F04/F06/F08/F11 的每个计算分支都能追溯到 PRODUCT 已确认条款。
- 当前仅D30/D32–D36待PRODUCT定案，受影响动态负向/上限、关系时间粒度、收款校区、退款、场地目录/受邀字段及规划导师管理范围不得验收。D24–D27已关闭：个人收入归集、职务资金独立、足额提现、社保到期待办确认扣职务豆、咨询管理费同项一次且默认2%均纳入验收。现金工资及原笔退款、比例最高权限等既有边界继续验证。
- D20 已确认不需要 Excel 导入；F14 只验收一致快照、完整导出、校验清单和受控下载。

### Gate B：领域与数据库

- 费率区间边界、当前上限、比例上限、缺失收款对象、正负余额、跨月和原期补差案例有确定性测试。
- 属性测试验证每项两位小数、最大余数分配的固定顺序、完整分配金额守恒；联盟动态负项是否存在及校验例外待D30，其余份额遵守已确认边界；缺收款人或比例错误的费用创建/编辑返回422，数据库无新版本、无净额/分润/ledger变化，编辑前成功版本保持有效。
- 余额验收覆盖100减120得到-20、再增加30得到10；API、微信、网页和 Excel 应一致保留符号与精度。此规则不撤销单笔分配剩余非负校验。
- PostgreSQL 集成测试覆盖个人/公司业务/场地账户隔离、昵称不可改、离职禁登、北京时间财年边界、特殊周、周三00:00截止、系统管理员延长后补原期、版本冲突、重复 requestId、整月并发重算、财年原子发布、序列化重试和不可变 delta ledger。
- 生源测试覆盖同名及同一现实学生在不同老师/课程下保持独立ID；A复制推B只复制允许字段并产生新记录、不复制费用；原A归档后激活保持原ID且21日重计；同一独立记录同周只能一笔有效费用，改错沿用fee_item_id留版本；输出老师能按范围查看推给A/B的各自全部费用。
- 同一人承接多个分润身份时，分类明细各自保留且余额正确累加；相邻财年报表互不混算，老师只见当前财年明细。验证上年期末100、新财年期内净变动20时，本年报表期初100、期内净增20、期末120；累计余额120，不清零为20，也不重复结转为220。
- 全部可配置费率及分润输入覆盖6.25%成功、6.251%拒绝、内部精确转换；负抬升配置保留历史；联盟负向实际计算待D30。周费显式0保存原行/零分配且余额不变，空白不视为0；0费用但非零比例缺对象仍拒绝提交。合并咨询管理费校验覆盖空值、0、100、负值、超过100及超过两位小数；0比例缺对象可提交，非零缺对象或总比例超100%拒绝且原版本不变。规则候选若使任一已有费用无效，整次发布拒绝并返回待补齐清单；不得创建部分生效版本。另测历史脏数据触发`FAILED_INVARIANT`不发布，证明它与规则预检失败不同。退款覆盖原实际分配唯一反向冲回、原单已退款、重复拒绝、其他笔不重新定档/分润及已提现余额变负；部分退款待D34，不将其列为已确认验收。
- 报销内部划拨验证：审批通过但未执行时两侧余额不变；财务 complete 后发起账户1000、申请老师20、金额100变为900/120，同一 `transfer_id` 只执行一次；期间老师另支出50后撤销该笔，余额为1000/-30，重复执行或重复撤销不得变化。奖金同测双侧原子、理由必填、负余额允许和任何计划不自动入账；工资另测现金确认的单侧扣豆。提现验证：本人余额100提80后为20且单据自动通过待转账；再提30提示“超出可用金额”，不产生成功单/扣款。提交后改撤被拒；财务标完成余额仍20。另例完成前财务撤回一次后余额回100，重复撤回不变；完成/撤回并发只有一项成功。零、负额、缺银行卡账户或姓名拒绝；余额100并发两笔80最多一笔成功。完成后更正走关联调账，不自动返豆。
- 人工调账验证：财务职责提交+20后余额立即增加、原子创建管理员审批任务并标记PENDING；审批通过不重复入账，拒绝/退回只冲回一次，财务创建者切换角色也不能自审。SYSTEM_ADMIN及实名授权开发者以SYSTEM_ADMIN职责提交时余额立即变化、标记NOT_REQUIRED、不创建审批任务且无审批入口；其原因、职责、授权来源和before/after完整可查。覆盖幂等、并发、越权、事务回滚和重算隔离。
- 基本工资测试覆盖4900循环两月后从选定月改5300：更早月不变，选定月及以后旧设置被新版本覆盖；未支付月只更新待办，任何计划任务都不扣豆或加豆。现金已支付4900并由财务确认后，老师个人账户仅写-4900，现金出资账户不写欢乐豆ledger；个人原有6000时余1100，其他账户余额不变。已支付月涨至5300只生成400差额待办，现金补发并确认后个人再扣400，余700；不扣第二次5300或4900。确认重试不重复扣豆；更正原4900扣豆仅追加一次关联+4900冲回，保留后续400及其他收支，现金支付事实不被标作撤销。奖金仍验证财务勾选填理由后双侧生效。生成任务覆盖Asia/Shanghai每月1日00:00、当月唯一及失败重试；医社保/公积金单侧方向验证：财务职务账户1000扣200为800，老师个人余额不变，且不生成个人收入；重试不二扣。到期待办不扣、确认才扣；改日期、短月有效日期、漏跑补办、重复生成与确认均不重复入账。
- REQ-021同项验证（无规划导师关系）：F=1000、联盟动态+1.5%、其余五项为6%/7%/5%/2%/1%时，学业规划介绍费100、合并咨询管理费20、联盟15、五项合计210、授课剩余655，总额1000；教师介绍120、咨询管理0，其他相同，授课仍655。一次费用只有一条现行CONSULTATION_CAMPUS_FEE，不另写旧Q或旧C，老师个人只获得剩余分润、不再追加20扣款。该算例仅验证金额；具体校区对象等待D33。历史Q/C映射前后ledger金额与ID不变。
- REQ-017分离与配置验证：校区分配记校区账户，校长个人业务分配记个人账户且看板分开，不自动抽成；所有分配参数有初始值且仅管理员可改；合并咨询管理费初始2%仅学业规划介绍适用、教师介绍为0，逐笔保留单一实际值与配置版本，不能同时按Q/C再收一笔；个别配置优先且不被新默认覆盖，六项默认只影响本财年采用默认的流水，历史Q/C原值及ledger不因统一类别映射重算；授课老师残值随其他参数变化，合计100%，超额发布被拒。


- REQ-022验证：三名老师各建场地后各见自己看板，一名老师多场地账户与余额分开，创建重试只建一个指定场地；无场地默认不能虚构账户；未接收记录录费也须有效场地，否则整笔拒绝无半账，复制给新接收方不沿用原场地授权/默认。授权查看老师不能提现、再授权、改比例或读所有者个人余额，跨venue_id直接请求拒绝；D35未定字段不扩散。F=1000在自己场地仍入VENUE50，个人仅计授课剩余；已有100的场地提80后仅该场地余20，个人与另一个场地不变，撤回只回原场地80且一次。当前周F=1000原场地A记50并已提50后，改B追加A−50/B+50，A余额−50；更早周不变，随后原笔退款只冲B50，不撤回旧提款。
- 规划导师分配验证：F=1000、有导师关系时介绍人PERSON80、规划导师PERSON20，介绍池100不另入账；无导师仍100，导师自荐100且导师子项0，无向上递归。动态15、组60、授课指导70、场地50、咨询校区20、平台20、分区10、授课655，十项总和1000；导师子项与指导7%分别列示，关系/比例篡改拒绝。管理员调抽取默认必须校验不超过介绍池，避免规划师净额为负；管理查看与关系时间边界D36单独保留。

### Gate C：API 与权限

- REQ-015覆盖：普通注册篡改role/scope被拒绝、仅管理员授权、右上角切换进入正确首页、撤权/迟到响应不泄漏；收入非角色项及0值隐藏、负值保留，隐藏不影响钱包/完整Excel。验证个人多个收入类别进入唯一PERSON余额、来源明细分列，职务账户资金不混入个人；换岗只改变授权、不迁移资金，奖金从职务扣豆给个人，同人两侧也不得合并抵消。手机号变更不转移资金归属，个人提现与个人入账锁同一PERSON账户；场地提现与该场地差额重算锁同一VENUE账户。
- REQ-020验收：学业规划为不授课个人身份；无规划导师关系的推荐费用1000时本人100、管理费20，教师推荐本人120、管理费0；动态正档2%另记联盟20，不改变介绍人100/120，完整剩余分配/对象待D30/D33。同区老师分配前F合计10000则校区营收10000，管理费另列，不用老师实得或管理费代替。周三管理员换组长，本周已记旧组长60冲回、新组长记60，上周保持不变；同笔随后退款仅冲回新组长60，不重复扣旧组长。分区改校区/导师/组长请求拒绝，管理员换校区后组/导师不变。校长/组长/授课指导导师分别只能读取所属人员的学生、逐笔费用、余额和非工资收入；工资及比例过滤继续执行。原笔F为1000且全额退款时，有效校区营收扣1000并另列退款。退款仅冲回原实际份额、状态已退款，另一笔费用/档位不被退款重算；操作权限/部分退款待D34。财务个人提交本人报销、切财务视角自审成功，管理员可查；不能据此自审人工调账或擅自开放对外付款自审。
- REQ-019权限验收：逐项验证全部非管理员的比例创建/修改/复制/批量/发布均被服务端拒绝；权限配置不能将CONFIGURE_RATE下放。四职务读取只含本人当前职责份额，成员列表、详情、历史、错误details、嵌套分配对象及同步不出现团队或其他岗位比例；同人兼任校长/组长切换后分别仅见对应份额，管理员身份未切换时仍受限。校长看到条件化校区管理费比例不改变个人收入归属。管理员完整配置及人员关系操作、分区名单与收入统计继续正常；管理员Excel保留全量比例，四职务无导出权限。
- OpenAPI schema 校验，miniapp/web 使用生成类型完成契约测试。
- 三类访问角色覆盖允许、禁止和越范围；账户类型、访问角色、分润身份不能互相授予权限。证明资料只读不妨碍已授权业务操作，只有系统管理员可改主资料和导出。验证总部财务/系统管理员工资可查、总负责人一般财务可查但无自动工资授权，分区/校区仅本范围及允许数据种类，管理层历年、老师当前财年；财务全局办理权限不能替普通用户发起提现。人工调账按请求时选定职责路由：财务必送管理员审批且本人不能通过，SYSTEM_ADMIN明确NOT_REQUIRED。实名授权开发者必须通过有期限、有范围、可审计的系统管理员职责账号操作，不存在隐藏后门。
- 字段级契约测试证明：总负责人/系统管理员/财务取得完整手机号、银行卡、发票和凭证；有独立资料能力的分区/校区管理仅在本范围取得脱敏手机号/银行卡，分区职责仅获名单所需最小资料、不获得银行卡或工资明细；校长/组长/授课指导导师在所属范围可查学生、逐笔费用、余额及非工资收入；老师仅本人和业务最小字段；越范围及非管理员导出被拒。日志清洗、会话撤销和管理员重置密码通过安全测试。

### Gate D：终端体验

- 微信开发者工具和至少一台真实手机验证登录、生源接收、周次录入、费用查看、审批和网络恢复。
- 手机网页和电脑网页完成同一业务流程，并分别验证触屏、大表格、键盘/鼠标、筛选和下载。
- 同一账号跨端写入后刷新，三端显示同一版本和结算结果。

### Gate E：Excel、迁移与恢复

- 按 Excel 规范验证 sheet、字段、行数、主外键、金额总计、快照时间和 SHA-256；只有系统管理员可下载包含完整敏感字段与凭证的全量导出。
- 使用含源数据异常、配置缺失、零值、停用、作废、历史版本和并发写入的合成数据导出；原数据全部保留，异常进入 manifest，不能因业务异常让备份失败。
- 验证只有快照取得、授权、复制完整性、文件生成或校验和失败才使任务失败；失败产物不能显示为完整备份。
- 验证北京时间周五10:00调度、周任务幂等、漏跑补建、手动任务隔离；附件原件及历史版本随包核对哈希；成功文件无TTL删除，短期下载令牌到期不影响永久留存；独立学生/生源及复制审计、工资基准/覆盖/生成版本、逐人权限、学年计划、全部计算版本和delta均全量导出。
- 在空数据库完成一次合成 `pg_dump` 恢复演练；恢复后重跑数据库和结算不变量。

任一必须 Gate 失败，不得标记为已交付或可上线。真实数据、真实支付和部署验收不能由模拟数据替代。

## 13. 分阶段技术依赖

这是依赖顺序，不是进度状态表：

1. **契约闭合**：先明确PRODUCT的D30/D32–D36，再固化受影响分配/关系/退款分支。账户、提现、月度待办确认扣豆及合并咨询管理费按现行已确认规则形成OpenAPI、错误码与Excel契约。
2. **工程基础**：建立 monorepo、CI、PostgreSQL 迁移、个人/公司业务/场地账户、三类权限、现行十类分润及旧Q/C历史类别、审计和合成数据。
3. **计划与原始业务流**：完成学年/period/周次/截止、老师/课程独立生源、复制推送、21日归档、原ID再激活、周费用版本和提交前强校验。
4. **实时计算与重算**：完成整月动态费率、两位小数守恒、原期补差、当前财年规则重算、旧财年规则隔离、规则发布有效性预检和delta ledger；余额跨年保留、期初承接上年期末但不再记一笔收入。
5. **财务调整**：实现财务调账管理员审批、SYSTEM_ADMIN调账NOT_REQUIRED、不可变ledger与冲回、财务全局办理范围、报销/奖金双侧内部划拨、现金工资个人扣豆和基本工资待支付计划；工资每月1日北京时间00:00生成待办，现金支付后财务确认单侧扣老师个人欢乐豆，补发只扣差额。完成足额提现自动扣豆、财务外部转账标完成及完成前唯一撤回返豆；医社保/公积金只扣财务职务账户，到期待办后财务确认扣豆，同月唯一且日期改动不二扣。
6. **多端与备份**：完成微信小程序与响应式网页全业务一致、每周/手动 Excel 完整导出和设备验证。
7. **迁移与上线准备**：Excel 导出验收和数据库灾备演练通过后，才申请真实资料、生产资源、部署和真实验收授权。

并行边界：miniapp/web 可在稳定 OpenAPI 下并行；身份组织、生源周费用、Excel 生成器可分模块并行；结算依赖费率、人员归属和周费用契约稳定；表7依赖 ledger；生产上线依赖全部前序 Gate。同一 schema 迁移和 OpenAPI 文件同一阶段只设一名写入负责人。

## 14. 部署前置条件与未定项

开始生产部署前至少需要：

- 微信小程序 AppID、主体和管理员、合法请求域名、隐私声明、体验版与审核流程；
- Web/API 域名、TLS、生产 PostgreSQL、私有对象存储、秘密/加密密钥管理；
- 监控、错误告警、审计访问、数据库备份计划、恢复演练、RPO/RTO 和保留期；
- 财年及业务时区已确认；明确性能目标、真实资料范围和运维责任人；使用 400 名老师、2,500 名学生的合成负载验证；
- 给出不超过 300 元/月的真实供应商报价和容量余量，经用户单独授权后才能购买或部署；
- 关闭会影响上线行为的 PRODUCT 待决定项；
- 安全审查、权限验收、真实设备验收和用户书面确认的计算样例。

当前未决为D30联盟动态负向与封顶，D32其他人事关系及寒暑假中途变更粒度，D33校区咨询管理费归介绍方或接收方校区，D34退款及自审报销查看等边界，D35场地选用目录及受邀看板字段范围，D36规划导师管理查看范围和关系生效粒度。场地独立账户、所有者按场地提现、普通周修改从当周重算、介绍池8%/2%已明确；D24–D29及D31保持关闭。所有未决以PRODUCT为唯一入口。

本轮明确规则已纳入；上列未决分支及生产容量、预算资源条件仍须落实，文档同步不代表业务实现或上线验收。

## 15. 官方能力依据

以下资料于 **2026-09-16** 核查；这里只引用能力边界，不锁定未经工程验证的依赖版本或价格：

- [Taro React 概述](https://docs.taro.zone/docs/react-overall)：确认 Taro 使用真实 React，并遵循小程序组件、API 和路由规范。
- [Taro H5 实现说明](https://docs.taro.zone/docs/implement-note)：确认其 H5 端和平台插件机制；本项目仍为桌面网页单独使用 React DOM，以满足管理看板需求。
- [NestJS Controllers](https://docs.nestjs.com/controllers)、[Validation](https://docs.nestjs.com/techniques/validation) 与 [OpenAPI](https://docs.nestjs.com/openapi/introduction)：确认模块化 HTTP 控制器、输入校验和 OpenAPI 集成能力。
- [OpenAPI Specification](https://spec.openapis.org/oas/latest.html)：作为跨端 API 描述和类型生成的标准来源。
- [PostgreSQL Numeric Types](https://www.postgresql.org/docs/current/datatype-numeric.html)：确认 `numeric` 适合要求精确值的计算。
- [PostgreSQL Transaction Isolation](https://www.postgresql.org/docs/current/transaction-iso.html) 与 [Explicit Locking](https://www.postgresql.org/docs/current/explicit-locking.html)：支持一致性事务、可序列化失败重试和行锁设计。
- [PostgreSQL pg_dump](https://www.postgresql.org/docs/current/app-pgdump.html) 与 [Backup and Restore](https://www.postgresql.org/docs/current/backup.html)：确认数据库一致性导出和恢复工具边界；官方亦明确提示常规生产备份不能只靠简单 `pg_dump`，因此上线前仍需制定完整备份方案。
