# DEV-010 小程序个人提现工程验证

范围为个人三类身份的提现来源、草稿恢复、真实文件字节适配、原件修订选择、提交与记录。使用共享客户端及已有提现服务，老师录费仍在前；没有接入微信支付或银行，也未使用真实账号、银行卡和原件。

## 证据与边界

- [组件适配测试](components.txt)：主任务最终6/6通过，5.93秒。其中一项检验文件字节识别，五项使用实际React组件、共享客户端和jsdom/Taro适配验证交互；HTTP和设备文件接口使用明确合成适配，不能当作微信真机或真实上传链路验收。
- [最终Taro开发构建](build.txt)：主任务使用所需本地权限构建成功，23.90秒。早期子任务受宿主Rust系统配置读取限制失败；主在同一最终源码上成功构建，不把失败改写为成功。
- [同期统一检查](../DEV-008-reimbursement/check.txt)：111/111通过，包含本包全部组件测试及工作区类型检查。

测试覆盖同槽v1替换为v2后刷新并精确提交v2；v2已READY但列表刷新失败时禁止使用旧v1提交；预留明确4xx解除上传锁，403清除敏感状态；未知创建不能凭发现同类草稿猜成功，原键仍可重试；未知提现保留同一冻结请求，原金额/收款资料/来源锁定，旧余额隐藏；实际父页刷新、退出、切身份都受保护，重试仍使用原键。

独立审查发现并修复旧附件误选、父刷新清掉未确认保护、未知创建失去重试入口及旧来源余额残留。主复核还补上上传READY后读取失败的旧版本保护和明确失败恢复。最终只读复核无未处理P1/P2。

测试直接依赖在小程序package中按已用版本声明，临时组件打包目录在Git忽略规则中排除且正常测试结束会清理。文件原件只使用本次临时字节和微信临时路径，不写长期本地存储。

复跑：`npm run check`；聚焦运行`node --test apps/miniapp/test/finance-attachment-helpers.test.mjs apps/miniapp/test/financial-panel.test.mjs`；构建执行`npm run build:miniapp:dev`。微信AppID、实际文件选择/上传/下载、真机交互与真实用户验收仍待对应环境。
