# 全量 Excel 备份导出技术契约

## 1. 目的、范围与非目标

本文为 F14“管理员全量 Excel 备份导出”提供实现和验收契约，承接每周自动备份、财年、账户和多端规则。本轮同步 [REQ-025](requirements/REQ-025-权限层级与采买划拨.md) / PRODUCT V1.22（2026-09-19）的退款后净额、权限层级、财务采买自动划转与审批证据；既有现金工资、招生分配和权限边界继续保留。

- **使用者与入口**：系统管理员及开发者本人可发起、查看全量备份任务及下载文件；老师和公司管理层只能在其授权范围查看资料，不能导出；这不取消已授权的录费、单据或财务业务操作。微信小程序、手机网页和电脑网页的业务规则一致；系统管理员与开发者本人可在各端以该端适合的方式发起异步备份、查看状态和受控下载，不能以手机屏幕或微信入口为由禁止已授权能力。生成始终在服务端完成。
- **全量备份**：按某一确定时点，导出所有业务数据、历史配置和支持理解关联与计算所需的元数据。它不受看板“金额为 0 隐藏”规则影响。
- **按期报表**：按月、年、组织或人员筛选的可读报表，是另一种导出类型；只能包含筛选结果，不能被称为全量备份。
- **非目标**：本文件不生成 `.xlsx`、不接入真实数据，也不提供 Excel 批量导入或恢复生产库的能力。数据库物理灾备、对象存储灾备和恢复演练仍是独立能力。

REQ-025层级例外：用户本人作为开发者（不自动涵盖任意开发人员或AI工具）是高于三类业务页面的最高系统所有者，可访问源码和全部数据并通过授权维护连接更正数据；只有开发者授撤管理员。完整导出同时允许系统管理员或开发者本人，使用独立SYSTEM_ADMIN/SYSTEM_OWNER授权来源；老师/公司管理层不能由查看范围获得导出。管理员可管理全部业务资料、工资、费用、身份和比例，但不获得源码权限。导出保留系统所有者身份/能力来源、管理员授撤、下级授权及维护前后值/理由/核验；源码、密码和连接密钥不作为业务备份内容。

## 2. 导出类型与管理员操作

| 类型 | 选择条件 | 内容 | 成功条件 |
|---|---|---|---|
| 每周自动全量备份 | 系统调度的当周执行窗口；无业务筛选 | 与手动全量备份相同的全量文件组 | 同一快照、调度幂等和 `manifest` 校验通过 |
| 全量备份 | 无业务筛选；系统记录 `asOf` 时点 | 第 3 节全部工作表、清单和校验信息 | 所有数据集来自同一快照，`manifest` 校验通过 |
| 按期报表 | 月/年、校区、分区、人员等已授权筛选 | 所选报表及其筛选说明；可含必要汇总和明细 | 与界面筛选口径一致，不能替代全量备份 |

手动操作采用异步任务：系统管理员或开发者本人在任一已授权端提交请求后得到任务编号；服务端固定快照、生成文件、执行备份完整性校验，再显示“可下载”。失败任务保留错误码、失败阶段和重试条件，不产生“成功”文件。重新下载同一成功任务不重新取数；重新生成必须创建新的快照和新任务。

### 每周自动调度

- 系统每周五 10:00（北京时间 `Asia/Shanghai`）自动创建一次全量备份，手动全量备份仍保留且不与自动任务互相替代。夏令时不改变北京时间的调度时刻。该调度、成功文件永久留存和历史数据永久留存均已确认；不得将其改为按浏览器时区或按管理员个人会话执行。
- 调度身份为受限的系统任务服务账号，使用最小权限读取快照和写入备份存储，不借用任何个人登录会话、下载令牌或过期凭据。它能生成备份，不能将文件直接发送给个人；每次下载仍按第 6 节重新验权。
- 幂等键固定为 `schedule_id + scheduled_window_start + export_type`。同一执行窗口只能有一个成功文件组；任务超时、可重试故障或进程重启时可安全重试，不能重复产生同一窗口的成功备份或覆盖已成功文件。
- 每次调度保留排队、运行、成功、失败、重试次数、下一次重试时间和最终错误；系统管理员在各端都能看到最近一次和历史执行结果。失败不伪造成功文件，且不得因一次失败跳过后续周任务。

## 3. 全量备份的数据范围

每份全量文件必须含 `00_manifest` 工作表和下表数据。业务表应输出稳定的内部 ID、业务显示名、创建/更新/删除状态、创建人/更新时间与版本号；老师昵称仅普通成员不可改，管理员/开发者更正需导出旧/新昵称、原因、操作者、时间及稳定person_id/account_id映射，不迁移余额。原始字段、审计 JSON 与长文本均须无损保留，不能仅导出看板可见列。

| 工作表或文件组 | 最低必须内容 | 目的 |
|---|---|---|
| `00_manifest` | 第 4 节字段、工作表清单、各表行数、逻辑摘要、总金额、时区、文件组编号 | 证明本次导出是什么及如何核验 |
| `01_组织、账户与权限` | 总部—分区—校区层级、`campus.region_id`当前外键及校区分区归属的完整有效期版本、个人账户、公司账户、每个校区独立公司账户、老师/公司管理层/系统管理员三类权限角色及逐人职责/范围配置历史（含总部财务、总负责人全公司范围及管理员财务审批可见权，分区职责任命/兼任、区内名单查看、管理员关系变更权限及历史（保留撤销前分区安排记录）、总部财务能力限制）；现行九类分润、学业规划个人招生身份、旧Q/C历史类别及历史分配独立保存；自然人/登录账号/账户映射、开发者专属管理员授予/撤销事件、管理员下级职责授权事件、独立公司钱包归属；每自然人唯一PERSON账户、每场地独立VENUE账户及职务/校区/联盟COMPANY账户映射，业务账户交接只变更授权（组长当周差额重算仍按F02） | 解释账户归属、权限与分润身份，不能将二者互相替代；不把职务业务余额算作负责人个人资产 |
| `02_表1_教师信息` | 表1所有非秘密原始字段、昵称及完整更正历史、离职/禁登状态、软删除和历史版本 | 还原主数据语义与关联；离职账号禁登但不删除 |
| `03_表2_业务流水` | 表2所有字段、老师/课程下独立学生记录ID及各自字段快照、接收方/输出方、仅供追溯的复制来源、推送/接收/已退款状态与业务状态变化、退款申请所选fee IDs/原因、总部财务审批或拒绝、逐笔批准时有效净分配及唯一退款反向记录、授课场地选择与版本、逐笔场地/收款/授课人及owner快照、自用标记、每记录每周累计费用及修订版本、周计划/归属月、21天计时起点/到期及重激活历史；不按同名或真实同一学生合并原始行 | 追溯费用来源和课程流转；没有“拒收”状态 |
| `04_分配明细` | 每笔流水的分配前`F`、推荐个人身份、介绍基础10%/教师12%、动态比例dynamic_delta_rate与有效介绍池、导师权重及介绍人/导师净份额、自用标记和场地配置/实际比例V、单一校区咨询管理费2%/0及介绍方校区快照、历史联盟动态项及现行停用依据、历史费率与旧Q/C原值、每一分润对象、金额、授课老师剩余、计算资格与排除原因、财年重算批次、改动前/后金额与账务差额 `ledger_delta_amount`、当时匹配的关系和配置版本，以及旧财年更正原期/原因/办理人 | 使表3/表7可逐笔复核，不能只导出汇总；关系历史永久保留；普通周组长、场地及规划导师关系仅当周及后续生成更正版本，更早周不变；暑期明确范围决定单独保存 |
| `05_表3_月度收入` | 表3原始/已发布汇总、期间、财年、汇总版本、重算批次和状态 | 复核月度口径 |
| `06_表4_财务单据` | 三类单据全部字段、审批状态、有效性结果、人工/系统自动事件与规则来源、业务单据及申请截图的READY不可变版本/hash/证据用途、状态历史与关联附件清单；报销含历史个人提交/财务自审记录；现行财务本人采买个人提交、自动通过和直接划转、无人工审批人的事实；另含当前视角与同一自然人、审批、执行人、发起支出账户、申请老师个人收款账户、金额、成对ledger的`transfer_id`及唯一反向冲回；提现含个人PERSON或本人拥有或获WITHDRAW授权的VENUE来源账户、场地ID、所有权或WITHDRAW授权及提交时有效版本快照、银行卡账户/姓名快照、自动通过、提交扣豆、待转账、外部转账完成及财务撤回唯一返豆、完成后关联原PERSON/VENUE来源账户的调账及其原审批路径；保留所有事件，不伪造银行到账 | 复核收入和支出调整 |
| `07_表5_工资奖金` | 工资资格选择及其有效期版本、计划、配置扣豆金额、现金支付事实、财务确认、个人扣豆及10项奖金内部划拨分列；工资保留老师个人账户、实际现金金额、支付/确认时间、办理人、理由、唯一确认幂等键、负向扣豆ledger、关联原笔的唯一反向冲回、计划修改前后值和差额补发关联，现金状态与账务更正状态分列；奖金保留发起职务账户、老师个人收款账户、双侧ledger、`transfer_id`、执行/撤销时间与名称快照。未来计划/待办不计入已入账金额 | 仅管理账务及管理员全量备份完整保留；分别核对工资单侧扣豆、奖金双侧划拨与计划，不按个人页不可见规则裁剪 |
| `08_表6_扣费` | 医社保、公积金、10项扣费、项目名称在该行生效的版本、历史名称与调整记录；医社保/公积金保留财务职务支出账户、负向ledger、成本归属人员、计划月份、财务设定执行日及版本/状态，不计入老师个人收支；其他个人扣费独立列示，到期待办及确认扣豆分列，不省略实际记录 | 解释扣费总和而不伪造方向 |
| `09_表7_费用结算` | 月/年/累计结算快照、各组成项、工资现金确认的个人扣豆及关联冲回、奖金/报销双侧内部划拨、人工调整净额（提交即生效，不能按审批通过过滤）、余额、跨财年累计余额、财年边界、结算状态和版本；旧财年更正差额与当前累计余额关联 | 复核最终余额；老师仅查看本财年不裁剪管理员全量数据 |
| `10_表8_绩效配置` | 各项初始默认比例、按接收方个别配置及其优先级、现行合并咨询管理费默认2%及实际采用值/来源版本、历史Q/C配置和逐笔原值、班型费率配置、有效时间、操作人、历史版本、复制/批量操作来源；全部比例配置写入正式限定SYSTEM_ADMIN当前视角且不可下放；导出仍包含全部历史配置及异常操作记录，不按操作者过滤，也不按四职务的本人比例读取范围裁剪 | 解释分配规则的时间点与编辑权限 |
| `11_费率、财年与期间计划` | 基础费率、动态区间、执行状态、历史上限及现行取消标记、费率历史版本、全财年重算批次、每年 9 月开始的财年、管理员年初生成的全年期间计划、寒暑假整周、管理员锁定的期间归属月、北京时间周二24:00默认截止及管理员延长记录 | 解释实时重算、周与月归属以及跨财年边界 |
| `12_账务调整、审计与调度` | 人工调账ID（财务、管理员及最高系统所有者开发者）、实际目标结算账户（PERSON或获准的原VENUE纠错账户）、调整前/后余额、带符号调整额、必填原因、提交人和时间、关联原单与ledger、上级审批任务/指定审批人、审批状态/意见/时间及完整历史（含待审批）、免审批标记与依据/执行职责（管理员免审及开发者最高维护能力记录）、拒绝/退回的唯一反向冲回ledger、重新提交与原调整的关联；工资现金支付确认、个人扣豆及唯一关联冲回，报销/奖金原内部划拨及唯一双侧反向冲回、旧财年财务更正、全财年重算记录、导出任务、每周调度执行与重试、登录外的管理操作审计、审批操作审计、执行人和时间 | 防止只看当前值而丢失变更轨迹 |
| `13_附件清单` | 附件 ID、所属业务 ID、文件名、介质类型、大小、内容 SHA-256、附件版本、`UPLOADING/READY/FAILED` 状态、存储状态和受限访问标识 | 使单据附件可被追溯；清单与原件同时构成全量备份 |

独立学生记录不因姓名相同、复制来源相同或真实为同一人而合并；复制关系只作审计，不能导出成共享学生主记录。未来工资计划应标注计划状态，不计入已入账金额合计；工资每月1日00:00（北京时间）生成当月待办，现金已支付给老师后由财务确认，仅扣老师个人等额欢乐豆；导出分别保留计划月份、生成时间、现金支付与确认事实、扣豆及更正记录。不得生成工资发起人扣豆/老师加豆的双侧划拨，现金出资账户也不另扣欢乐豆。涨薪差额待办不入账，现金补发确认后仅扣补发差额；重复确认或冲回只保留一次有效账务变化，账务冲回不代表真实现金已退回。

教师个人页不展示现金工资模块或工资扣豆明细，是服务端访问权限限制，不是删除账务。工资资格、配置、现金实发、扣豆及关联冲回仍完整进入管理员全量备份；个人余额仍按含工资扣减的完整有效账务计算。总部财务可在其管理视角查看工资，但不因此获得仅管理员/开发者拥有的完整导出权限；分区管理职责可看区内人员名单和营收，不能改人员关系、查看工资或导出。兼任者在教师/分区视角也不能取得工资明细，须切到获授权总部财务/系统管理员视角。

用户汇总表的零值和非角色项隐藏不影响完整备份：已存的全部身份收入、零值、撤销角色历史及授权审计均保留。REQ-021已确认每个人的全部个人收入归唯一PERSON账户；职务/校区/联盟业务资金独立，换岗仅改授权不转移原资金；备份按实际账户与职责分类完整列出，公司账户与个人账户不得混合，明细保留角色/身份归属以解释余额。校区管理费对应独立校区账务主体，分配前F总营收仅为统计，校长以老师身份取得的业务分配对应其个人账户与个人页；不自动从校区抽成或新增校长固定分润比例，导出按两个主体分别核对。

REQ-020校区总营收取本校区所有老师的分配前课时费F合计，已退款原笔从有效营收扣除且退款另列，原费用行保留；校区管理费独立列示；分区总营收为各校区同口径F合计，自身分润另列，查询聚合不生成新ledger或重复入账。备份保留F、管理费、分区自身分润、历史归属和已发布版本，不再用旧校区2%管理费作为营收。分区关系写入已收回至管理员；管理员变更记录及有效期保留，普通周组长保存生效周；其他关系及寒假中途变更粒度待D32；暑期中途变更导出管理员明确的范围、决定理由和影响记录，组长更换重算当周及后续、不影响更早周，校区变更不带动组长/导师。


REQ-023场地导出保留venue_id、创建/所有者、独立VENUE账户、VIEW/WITHDRAW逐场地授权与撤销历史、默认选择、费用授课人及所有者快照、自用标记、配置场地比例与实际适用比例/金额。自用0收入且增加授课剩余，他用初始5%；当周自用/他用互换保存受影响PERSON与VENUE差额及旧版本，更早周不变，已提款负余额保留。授权提现不转移场地资金归属、不带来修改场地/再授权/配置/调账能力；提交时权限证据、申请人、银行卡姓名、原VENUE来源、扣款/完成/原账户唯一返还均完整留存。授权撤销不自动撤销既有成功申请。未接收录费仍须实际有效场地，复制到新接收方重新校验选择/默认，不回退表1场地人或虚构免收场地；共享VIEW可见场地授课老师/学生、各老师周场地费、总使用费、可见老师及权限；自用真实使用及0费用仍保留，总使用费不是可提现余额；仍不见个人余额/银行卡/工资/全表比例；D35仅保留选用目录未定。

REQ-024规划导师与授课指导导师7%分列。导出管理员职责任命、导师自行增减旗下规划师的操作、旧/新关系及生效版本，解除关系不删自然人或历史。介绍池保存R_base、接收方净额、delta、R_eff、显式导师池内权重w（初始20%）、旧M_base/R_base兼容映射依据、实际介绍人/导师金额；例10%−2个百分点=8%后净6.4%/导师1.6%。无导师或导师自荐w=0、取全有效池且导师子项0，不重复记介绍池。备份验收同时核对原15档命中与未封顶结果：最低10−2=8%并拆6.4%/1.6%，最高教师12+4.5=16.5%而非16%。基础为0时仍按R_eff=R_base+delta计算，不自动关闭动态；旧M_base/R_base仅在基础>0映射，基础0必须有明确权重，缺失拒绝发布，不由导出推测0/0。当前九项与历史十项/旧Q/C/ALLIANCE_DYNAMIC按版本完整保留；D30已关闭，原15档区间及数值现行执行、不反号，16%封顶已取消；规划导师关系从调整当周起重算、更早周不变，D36关闭，暑期中途由管理员明确范围。

运营校长、分区财务、场地运营、教研组长在服务端仅可读取本人当前职责或所拥有场地对应比例，不包含团队成员/其他岗位比例或完整分配表；兼任时按当前职责隔离，切换到管理员才可查看全量配置。该限制不改变管理员完整Excel范围：全部默认、个别、历史比例、来源及异常操作都应保留，用于审计和金额核验；不因个人或职务页不可见删除字段。校长可见校区职责比例不改变校区账务归属。四职务无导出权限，其收入统计及名单查看能力不能转为配置或全量比例读取授权。本条不扩张其他未提岗位的比例读取范围；所有下级非管理员业务职责不能修改比例，开发者最高维护能力按REQ-025留痕。

REQ-024动态正负浮动已回到介绍费池，现行不再记联盟动态收入。基础规划10%/教师12%，delta按接收老师净额命中原15档形成R_eff，有规划导师按原8:2权重分池；咨询费与平台费等其他比例不随介绍池浮动。咨询费给实际介绍规划师本人校区，导师自荐取本人校区，历史有效归属不被当前校区覆盖。历史联盟动态、Q/C、旧基础/档位/上限及原ledger全部保留，不能因新算法删除旧账或重复生成新账。

“删除”不得等同于从全量备份省略：软删除、撤销、拒绝、关闭、历史版本和零金额行均须导出，并以状态列说明。页面为了易读而隐藏零金额项目时，仍须保留机器可读原始行和金额字段。

欢乐豆余额（表7最终余额）允许为负数：负值及负号必须在精确值和展示值中一致保留，不归零、不隐藏，也不单独作为业务异常或导出失败条件。历史业务记录、历史配置、软删除行和软删除行关联的附件原件均永久留存；离职、停用或软删除不能触发销毁。

财年按北京时间从每年9月1日00:00开始，到次年9月1日00:00之前结束。老师的界面只显示本财年，但管理员全量备份必须同时包含所有财年、已锁定的周归属月、跨财年累计余额及历史版本，不能因前端可见范围裁剪。旧财年由财务更正时，导出必须保留原期、原期规则与人员关系、原因、办理人、新旧差额和当前累计余额影响；不得将旧年度收入改写为当前年度收入。费率规则修改后只实时重算整个当前财年时，备份必须保留重算批次、触发配置版本、原金额、新金额和差额，不能只留下重算后的当前值；组长变更以当周为界重算当周及后续关系，保留旧/新收款对象和差额，不重算更早周；其他人员关系调整由管理员记录生效信息，不能无痕改写历史。

### 首版附件原件与文件包

首版支持发票等对应业务单据、申请截图及付款凭证等业务附件上传，并将附件原件随全量备份下载。一个成功备份文件组由 `.xlsx` 工作簿、`package-manifest.json` 和 `attachments/` 原件目录组成；可作为一个 ZIP 包下载，但 ZIP 只是交付容器，不能替代其中逐文件校验。

- 每份附件在导出快照中必须记录 `attachment_id`、所属业务记录、附件版本、字节大小、介质类型和内容 SHA-256。导出读取的是与同一数据库 `snapshot_id` 对应的附件版本；原件按不可变字节保存，不能在生成中被同名新上传文件替换。
- 以快照内上传状态为准：已完成上传的 `READY` 附件原件全部必需。任一READY原件无法读取、版本不匹配或内容哈希不符，本次备份为**不完整失败**，不得标记成功。上传中 `UPLOADING` 或失败 `FAILED` 的记录完整导出状态及原因，并在清单标为原件未就绪，不能用占位记录、外链或缩略图冒充已备份原件；未形成原件不阻断已存数据的完整备份，也不得将该附件标成已成功备份。
- REQ-025所有人的财务审批及自动通过都必须附对应业务单据和申请截图，正式提交/批准/自动通过及入账前两类原件须READY，证据不完整仅可存草稿；业务单据不是事后付款回执，不要求事前提供尚未发生的回执。历史缺附件业务记录仍原样导出并列业务异常，不虚造证据；快照内已存在READY原件缺失则备份失败。比例错误、缺少分润收款对象会阻止提交；失败校验审计及已有异常数据仍遵循“原样导出并标异常”的规则。必需附件缺失属于备份内容缺失，不是可带着成功的业务异常。
- `13_附件清单` 的每一条READY版本都必须与 `attachments/` 中一个同哈希原件对应；软删除、作废或历史业务记录的原件仍归档。清单从来不等同于附件原件。

REQ-024退款备份保留授课老师申请、学生/接收记录、原因、范围、总部财务审批/拒绝及时间、批准所选范围的原有效净分配快照和唯一冲回。申请/拒绝不计入退款已入账合计，批准后逐项关联原ledger并标已退款；申请冻结所选一笔或多笔原周费用ID，批准逐笔全额冲回，不默认整学生全退或提供任意部分金额；REQ-025确认下次正常新增/改错的整月计算从接收与输出两侧排除已退款费用，按退款后净额重定有效费用，已退款不得恢复或再次冲回。原记录永久保留，退款批准当次不重新套当前比例或重分其他笔，换组长/场地/规划导师已生效差额计入退款前有效净链。线下现金退款不作为系统支付记录，也不添加家长账户，系统批准不声称现金已退。历史本人报销自审记录仍保留，财务其他本人报销仍允许原手工自审后执行且须完整证据；现行财务本人采买保存个人页申请、系统自动通过及直接划转事件，无人工审批人，财务业务账户−N、申请人个人账户+N，保留双方原账户ID、同一transfer_id及唯一反向冲回，不以申请流转方向代替资金方向。开发者/管理员全量查阅，下级管理老师仅在授权范围查看。

### 业务异常保留原则

全量备份复制的是某一时点的真实已存数据，不是“把数据修正后再导出”的报表。配置缺失、业务金额不平、历史悬空引用、计算差额、旧版本字段不完整和其他业务校验异常，都必须原样导出；同时在 `12_账务调整、审计与调度` 或独立 `14_业务异常` 工作表写出异常编号、涉及记录 ID、规则版本、发现时间和说明。

人工调账提交即生效，待审批记录及关联余额变动必须完整导出；审批状态与入账状态分列，拒绝/退回的反向冲回也单独保留；净额从原调整与冲回账务汇总，不能删除原调整或再次按状态扣一次。上级能随时查阅调整记录不改变仅系统管理员与开发者本人拥有的完整导出权限。

周录入无需财务确认；校验成功提交的周数据进入计算。比例错误或非零比例缺少收款人会阻止创建/修改，不生成新的有效费用，不影响净额、档位、分润或余额；修改失败保留原成功版本。失败输入和原因作为独立校验审计导出，与有效费用分开、不重复汇总。已有异常数据和历史审计仍原样导出并标明原因，不因正常流程已拦截就省略。

无效、拒绝、作废等业务状态记录同样保留；正常发生的状态本身不等于数据损坏或计算异常。

导出程序不得自动补数、重算覆盖、合并、删除或隐去异常行。备份内容完整不等于业务计算正确：业务校验状态与报表汇总核对状态独立记录，不能因业务异常使全库备份缺行或失败。

### 不导出的秘密与敏感字段

- 密码哈希、密码重置令牌、登录会话、API token、私钥、数据库连接串和任何密钥一律不导出。
- 设计要求管理员获准导出的手机号、银行卡号、开户行、收款户名、发票号等业务字段按原始文本保真；完整业务字段授权已确认，系统管理员和开发者本人可以导出，下载、失败和撤销均写入审计记录。
- 卡号与手机号均以文本列保存，不能转为科学计数法、自动去前导零或按公式计算。非系统管理员不可导出已确认；查看时按PRODUCT F01区分完整字段与脱敏范围，业务数据永久留存已确认。

## 4. 快照、清单与一致性

导出任务开始时建立只读一致性快照，所有工作表只能从该快照读取。快照结束后新增或变更的数据进入下一次导出，不能混入当前文件。

`00_manifest` 至少包含：

| 字段 | 含义 |
|---|---|
| `backup_id`、`export_job_id`、`file_group_id` | 本次导出与多文件组的唯一标识 |
| `export_type`、`schedule_id`、`scheduled_window_start`、`schema_version`、`application_version` | 手动/自动导出类型、自动任务执行窗口和字段解释版本 |
| `as_of`、`timezone`、`snapshot_id`、`covered_fiscal_years`、`period_plan_versions` | 固定数据时点、IANA时区、一致性快照、全部覆盖财年及期间计划版本集合 |
| `requested_by`、`requested_at`、`authorization_scope` | 谁在什么权限范围内请求；不记录密码或 token |
| `filters` | 机器可读筛选条件；全量备份显式为“无业务筛选” |
| `sheet_name`、`part_no`、`row_count`、`min_id`、`max_id` | 每个工作表/分片的覆盖范围与行数 |
| `logical_digest`、`total_beans`、`reference_check_result` | 按稳定排序和规范化值计算的逻辑摘要、欢乐豆金额合计和引用检查结果 |
| `fiscal_recalculation_batch_ids`、`rate_version_ids` | 同快照涵盖的全财年重算批次及费率历史版本 |
| `attachment_manifest_digest`、`attachment_ready_count`、`attachment_unready_count` | 附件清单逻辑摘要、可交付原件数量和未就绪数量 |
| `package_manifest_ref` | 外部文件清单的相对路径；完整清单SHA-256仅存任务审计，不写回任何被该清单计算文件哈希的工作簿，避免循环依赖 |
| `generated_at`、`generator_version`、`backup_status` | 生成时间、程序版本和备份完整性状态 |
| `business_validation_status`、`report_reconciliation_status` | 业务异常汇总状态、报表汇总核对状态；二者不决定是否复制原始行 |

导出完成后，服务端生成并随文件组提供只读 `package-manifest.json`，其中列出每个 `.xlsx` 和附件原件的相对路径、文件名、字节 SHA-256、文件大小、工作簿逻辑摘要、附件版本及全组必备分片；服务端任务审计也保存同一清单及其SHA-256，供离线留档时核验文件组是否完整或被改动。工作簿内保存的是各数据集的 `logical_digest`，不把整份文件哈希写回自身，以免形成自引用循环。时区必须使用系统配置的 IANA 名称，并同时作用于 `as_of`、日期时间显示和期间边界；上线前固定该配置，不能由浏览器时区决定。

## 5. Excel 文件格式与大数据策略

Microsoft 规定单个工作表最多 1,048,576 行、16,384 列，单元格最多 32,767 个字符，且数字只有 15 位精度；因此导出不能依赖一个无限大的工作表，也不能把身份证明、精确金额或超长业务资料交给 Excel 自动处理。见已核实可访问的 [Microsoft Excel specifications and limits](https://support.microsoft.com/en-us/excel/excel-specifications-and-limits)。工作表名称还不得超过 31 个字符或含非法字符，见 [Microsoft Rename a worksheet](https://support.microsoft.com/en-us/excel/rename-a-worksheet)。

- 每个工作表最多写入 1,000,000 条数据行（另加标题）；超出时按稳定主键顺序拆为 `03_表2_业务流水_001`、`_002` 等分片。文件整体达到服务端配置的大小或生成时限时，拆分为同一 `file_group_id` 的多个工作簿，每一份都有完整 `manifest`。
- 后台按游标分页读取并流式写出，禁止先把全量记录加载到服务端内存。若技术栈采用 Node.js，ExcelJS 的官方仓库提供 streaming XLSX writer；选型和版本锁定时再以其 [官方文档](https://github.com/exceljs/exceljs#streaming-xlsx) 验证兼容性。
- 文本型 ID、手机号、银行卡号、昵称、发票号、精确金额和比例均以文本写入。所有欢乐豆账务金额以欢乐豆为结算和展示单位，`1 豆 = 1 元`；工资另保留现金已付金额与支付时间作为事实字段，不混作欢乐豆收入；输入/入账业务金额两位小数，`*_beans_exact`与`*_beans_display`保留同值及正负号。对计算中间值、历史异常值，另以`*_raw_decimal`无损保留源精度，不得强制截成两位小数；比例同样保存精确文本。不允许Excel自动舍入、科学计数法、归零或改符号，不为欢乐豆金额另设人民币换算显示列；工资现金支付事实金额可独立保存并明确单位元。不在文件中放依赖 Excel 的业务公式。
- 所有业务文本显式写为 XLSX 字符串单元格，尤其首字符为 `=`, `+`, `-`, `@` 时禁止写为公式类型，也不插入会改变值的前缀；在 `manifest` 标明 `text_encoding=xlsx_string_v1`。读回值必须与源文本一致，并核验不存在来自业务输入的公式单元格。
- 单元格超过 32,000 个 UTF-16 代码单元的审计 JSON、备注或其他原始长文本，主记录保留 `long_text_ref`，完整文本写入 `14_长文本分片`：`long_text_ref`、源表、源记录 ID、字段名、`part_no`、`part_text`。每片控制在 32,000 个 UTF-16 代码单元内，边界不拆开代理对；按 `part_no` 顺序拼接必须逐字符等于源值；不得截断、摘要或以下载链接替代。若不能新建该工作表、写入任一分片或写入后重组校验失败，备份任务失败，不能降级为截断成功。
- 工作表名、列名、枚举值和字段版本需由导出 schema 注册表统一维护。`sheet_id` 必须是 31 字符以内的合法且唯一技术名，完整中文逻辑名写入 `manifest`；名称冲突或无法分配合法新工作表时任务失败，不能截断名称后覆盖另一工作表。新增列为向后兼容追加，删除或改义必须提升 `schema_version`。

## 6. 生成、下载与留存安全

1. 服务端在手动创建、自动调度开始和开始生成前校验SYSTEM_ADMIN或开发者本人SYSTEM_OWNER导出权限；自动调度校验的是系统服务账号授权范围而非个人会话。生成过程若请求者对应的SYSTEM_ADMIN或SYSTEM_OWNER访问授权失效则标记对应手动任务失败并删除未完成临时文件；系统调度任务按受限服务账号规则继续受审计控制。
2. “下载”必须经过受控服务端端点：在响应开始前再次校验权限、任务所有权/管理员或系统所有者范围、文件状态和一次性下载令牌。不能把对象存储的长期裸链接交给浏览器。
3. 下载流开始后，系统持续检查撤销标记；角色被撤销时中止尚未完成的传输并审计“已中止”。已经发送到客户端的字节无法收回，此限制必须如实记录。
4. 下载令牌仅可使用一次，并使用短有效期。**建议**有效期 15 分钟；该数值不是当前已确认的保留或成本承诺。每次成功、失败、过期、撤销与清理均写入导出审计。
5. 成功备份文件组（`.xlsx`、`package-manifest.json` 与附件原件）、历史业务数据和历史附件原件永久留存，不设成功文件 TTL 或自动清理。失败任务的临时写入文件可以在诊断信息落库后清理，但不得触碰源业务数据、成功文件组或成功附件原件。短效下载令牌继续独立于永久留存生效。
6. 永久留存会占用持续存储容量；月度运行预算约 300 元是容量、存储策略和告警设计约束，不是“无限免费”的承诺。本轮不采购服务、不部署，也不以预算数字削减已确认的永久留存范围。

## 7. Excel 导出与数据库灾备的边界

Excel 全量导出是管理员可查看、离线留档和交叉核验的数据包；它不等同于数据库物理备份，也不能自动证明可恢复生产服务。本阶段明确**不提供 Excel 批量导入**。生产级灾备还需要数据库备份、附件原件存档、密钥管理、恢复权限、恢复演练和恢复目标。

附件原件随全量备份文件组提供，见第 3 节；但这不等同于数据库物理备份或生产恢复能力。公司管理层的历史资料查看范围尚未明确，本规范不因系统管理员拥有全量备份而扩大其历史访问权限。

## 8. 验收与自动核验

备份失败与业务异常必须严格区分。权限不足/撤销、无法建立或保持一致快照、源读取或分页缺失、schema/分片/长文本重组失败、逻辑摘要或行数不一致、文件写入/可打开性失败，以及快照内READY附件缺少原件、附件版本不属于快照或任一内容哈希不符，均为**备份失败**，不得产生成功文件。配置缺失、业务计算差额、历史悬空引用等为**业务异常**：备份仍应完整完成，`backup_status=COMPLETE_WITH_BUSINESS_ANOMALIES`，并在异常工作表与 `manifest` 中呈现。报表汇总不一致只能使 `report_reconciliation_status` 失败，不能删除原始行或把完整备份降级为空。

每次生成成功文件前，服务端必须完成并记录以下检查：

| 检查 | 通过标准 |
|---|---|
| 快照 | 所有数据集来自同一`snapshot_id`/`as_of`；清单列全覆盖财年和期间计划版本，每行保留其各自版本，不要求不同年份相同；无读取超出快照的行 |
| 首尾与排序 | 每个分片的首/末主键、分页游标连续，合并后与快照查询的稳定排序一致 |
| 行数 | `manifest` 行数等于源查询行数；分片行数之和等于对应数据集总数 |
| 欢乐豆金额 | 各分配明细以两位小数的 `*_beans_exact` 汇总后，写入表3/表7及 `total_beans` 的核对差异；负余额、差异和排除原因均保留，不能静默四舍五入、补数或掩盖 |
| 引用与账户 | 可解析引用通过引用核对；个人/公司账户、校区独立公司账户及每场地VENUE账户、三类权限角色、现行九类分润及旧Q/C历史类别均可按稳定 ID 追溯。历史悬空/已删除/缺失引用须保留原始外键并列为业务异常，不得省略该行 |
| 人工调整 | 调整前余额加带符号调整额等于调整后余额；提交记录、ledger及需审批时的审批任务可追溯；免审记录须明确标记且不伪造审批人/通过事件，待审批不从余额汇总或原始记录中省略；拒绝/退回只冲回一次，重新提交关联新调整与审批，原调整和冲回均保留 |
| 工资与校区分离 | 计划/待办不扣豆；现金已付且财务确认的每笔工资仅有老师个人等额负向ledger，补发只扣差额，重复确认/冲回幂等，工资无发起方欢乐豆扣款、无老师加豆；冲回保留现金事实；奖金/报销仍按双侧核对；工资与课时分配独立，校区公司收入与校长个人业务分配分列不抽成 |
| 分区聚合与工资完整性 | 校区历史归属及已发布版本可追溯；校区营收=分配前F之和扣除已退款原笔，退款另列且保留原行；分区总额为各校区有效F合计，管理费/自身分润另列且无聚合新增ledger；教师页不展示的工资资格/配置/现金/扣豆/冲回均完整导出，与真实余额可核验；分区财务或教师视角无导出权限 |
| REQ-021账户与办理 | 同人多类个人收入合并到唯一PERSON余额，场地VENUE及职务业务资金独立且换岗不迁移；奖金两侧、PERSON/VENUE按来源各自核对的提现提交扣款/完成不二扣/财务撤回一次返还、超额失败无成功单及社保到期待办确认单侧扣职务豆均可逐笔核验。银行卡及姓名快照完整保留在管理员受控备份，不伪造银行交互 |
| 分配默认配置 | 保留管理员初始值/历史版本、老师个别优先来源、现行合并咨询管理费实际值及来源；旧逐笔Q/C原输入与实际结果永久保留，类别映射不改历史ledger，现行只收一次。授课残值与总额可核验，完整备份不按四职务仅见本人比例裁剪 |
| 场地与规划导师 | VENUE账户不并PERSON且提现/返还按原来源；仅VIEW授权不能提现或导出；WITHDRAW授权也不包含导出；当周自用/他用切换的授课PERSON和旧/新VENUE差额与退款有效净链可追溯，已提款历史保留。介绍池按基础加delta确定，规划师/导师按8:2权重只各入账一次，导师自荐取全有效池不自抽；九项合计守恒；自用/他用切换后按统一尾差算法产生的第三方0.01豆差额也须完整保存 |
| 退款与换岗 | 退款前最新有效净分配和退款反向流水逐项对应、只冲回一次，退款当次不重算其他笔；后续正常新增/改错按两侧排除退款后的净额重算有效费用且原退款不恢复；组长变更从当周重算旧/新对象差额，更早周保持不变；管理员关系操作完整留痕、换校区不改变组/导师 |
| 财年重算 | 每个费率版本改变后的当前财年重算批次都有旧值、新值、`delta`、触发版本和期间计划；跨财年累计余额与已锁定月归属可复核 |
| 自动调度 | 每周执行窗口在同一 `schedule_id` 下只产生一个成功备份；重试不产生重复成功组，失败及重试状态对系统管理员可见 |
| 附件原件 | 快照内全部 `READY` 原件与清单一一对应，字节 SHA-256、大小、版本全部匹配；缺原件或哈希不符不能通过。`UPLOADING/FAILED` 记录保留原状态和原因，不能标成原件已备份 |
| 异常任务 | 无法匹配、配置缺失、金额/比例校验失败生成业务异常；导出中权限撤销、快照/分页不完整或文件写入失败才使备份任务失败 |
| 文件可用性 | 每个 `.xlsx` 可打开，工作表/列与 schema 一致，文本、前导零、精确金额和公式转义样例保持预期 |
| 安全 | 老师、公司管理层、过期令牌、重复令牌和已撤销系统管理员均无法取得文件；审计可关联手动请求、自动调度、下载和清理事件 |

验收样例至少覆盖：唯一个人余额与独立职务资金、多个场地独立余额、所有者和WITHDRAW受邀提现、VIEW单独不含提现、受邀不可再授权、当周自用/他用换场地的PERSON与VENUE差额及更早周不变、规划介绍池动态后按8:2权重及导师自荐不自抽、导师关系当周起重算且更早不变、奖金职务到个人、超额提现失败、提交扣豆/用户不可改撤/财务完成不二扣/完成前撤回唯一返豆、社保到期待办不扣豆及确认仅扣财务职务账户、月度改日期/重试不重复、规划基础10%/教师12%加delta、无导师取有效池且咨询费规划2%/教师0、当前无联盟动态项而历史保留、F营收与管理费分列、所选一笔或多笔周费用申请/审批及逐笔全额唯一冲回、财务本人采买个人提交/自动通过/直接划转（财务业务账户1000−100=900、个人20+100=120，重试不重复，关联冲回原账户）、所有人财务审批两类READY证据、其他本人报销仍可手工自审及历史保留、四职务本人比例字段隔离但管理员备份全量、切换职责不混入其他岗位比例、比例写入不可下放、教师个人视角无工资明细但全量备份完整、分区职责无导出权限、管理员人员关系变更、组长当周重算及更早周不变、校区跨区历史归属、分区自身分润独立列示、分区总和不重复入账且不再加自身分润、工资计划未支付、现金已付确认单侧扣豆、确认重试、补发差额、关联冲回且现金事实保留、校区与校长个人分开、默认与个别配置优先级、合并咨询管理费只收一次及旧Q/C历史保留、零金额、负欢乐豆余额、离职禁登和软删除记录、3 周未接收后再次推送激活、跨月周流水、寒暑假整周、管理员锁定归属月、跨财年累计余额、费率全财年重算和 `delta`、个人/校区公司账户、权限角色与分润身份分离、周五 10:00 北京时间自动任务、不同文件分片、超过 15 位文本、以公式字符开头的文本、超过 32,767 字符的审计 JSON/长文本及重组、业务计算差额、比例错误、缺少收款对象、历史悬空引用、自动任务重试、`READY` 发票/付款凭证原件、`UPLOADING/FAILED` 附件、软删除记录的历史原件、附件哈希篡改、导出中撤销系统管理员权限及永久成功文件不被清理的情形。[问题登记](../product-log/ISSUES.md)中尚未明确的业务口径不在本文件中被推断；其异常应被显式呈现，不能被导出流程吞掉。

## 9. 实施前置条件与待确认边界

- **D20（Excel 导入恢复）**：用户已明确本阶段不需要导入；本规范只定义全量 Excel 导出及完整性核验，不设计恢复接口、导入流程或导入验收。
- **D21（自动备份留存与附件）**：已确认每周五 10:00（北京时间）自动全量备份、成功文件/历史数据永久留存，以及首版发票/付款凭证附件上传与原件随备份文件组下载。对象存储实现、容量告警、预算内资源配置和生产恢复目标仍待后续工程细化。
- **D24（已关闭）**：个人所有收入归本人唯一个人账户并保留职责分类；职务、校区及联盟业务资金独立。奖金职务扣款/个人加款，权限交接只变更授权、不整体转移余额；组长变更当周及后续差额仍按F02重算，更早周不变；备份完整保留主体、授权及两侧账务。
- **D25（已关闭）**：本人PERSON或本人拥有或获WITHDRAW授权的VENUE的足额正数提现提交即自动通过扣来源账户豆，超额失败不产生成功单或扣款；用户提交后不可改撤，银行卡账户及姓名快照保留。银行交互在系统外，财务转账后标完成不二扣，完成前财务撤回只向原来源账户返豆一次，完成后纠错另走调账；全部原始事件和账务完整导出。
- **D26（已关闭）**：医社保、公积金到财务设置的每月执行日期只生成待办，确认后仅扣财务职务账户；个人余额和收入不变。导出保留计划、执行日/版本、人员成本归属、待办、确认及唯一负向ledger，日期修改和重试不得造重复月度记录；不伪造外部现金支付。
- **D27（已关闭）**：咨询平台费与原2%管理费同项一次，学业规划介绍默认2%、教师介绍0，给校区且从授课剩余承担；现行统一类别和旧Q/C历史原样保留。D33已关闭，收款校区为实际介绍的规划师本人校区（导师自荐取本人），不取接收方或抽成导师校区。
- **D28（已关闭）**：全部分配参数正式限定管理员修改且不可下放；运营校长、分区财务、场地运营、教研组长只看本人当前职责份额。管理员完整备份仍保留全量比例、历史及操作审计，非管理员无法通过报表导出绕过读取限制。
- **D29（已关闭）**：分区自身分润保留原1%默认且管理员可调；分区可查看自身收入、辖下名单及营收，人员关系调整按REQ-020统一由管理员办理；备份保存全部历史操作，不因旧权限被撤销删除历史。
- **P01、D32、D35**：分区结算查看范围、其他人事关系/寒假粒度、场地目录仍待[问题登记](../product-log/ISSUES.md)明确；分区不因待定查看权获得导出权限。D34已关闭：本人采买财务业务账户扣豆、申请人个人账户加豆。D30关闭且当前15档区间/值执行、取消16%上限、暑期交管理员明确范围、退款按勾选原周费用逐笔全额财务审批、共享字段和规划导师介绍链查看均已定。D36关闭，导师关系从当周起重算；D33关闭，咨询费给实际介绍方本人校区。现行九项和历史类别按规则版本保存，不删除旧记录。
- **运行策略**：系统管理员下载令牌短期有效与成功文件永久留存是不同配置；月度约 300 元预算用于容量核算，不构成部署、采购或无限免费承诺。
- **技术选型**：架构方案拟采用 PostgreSQL；本规范要求“一致性快照”这一结果，并计划使用其事务快照能力，见 [PostgreSQL `pg_export_snapshot`](https://www.postgresql.org/docs/current/functions-admin.html)。该方案尚未实现，具体查询、并发边界、压测和恢复演练须在开发阶段验证。

## 10. 与原需求的对应关系

V1.6 第 6.3.1 节授予管理员“所有表查看与导出”权限，第 6.4 节将“报表导出、按月/年查询”放在第四阶段；原文没有规定 Excel、全量范围、历史数据、下载安全、附件、留存或恢复。本契约新增的是管理员的全量 Excel 备份导出实现边界，不改变 F01–F11 的计算公式、数据权限或真实付款规则。
