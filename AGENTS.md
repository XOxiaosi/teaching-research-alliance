# 教研联盟管理平台 Agent 工作规则

## 三份文档的职责

| 文件 | 唯一职责 | 不在这里维护 |
|---|---|---|
| [AGENTS.md](AGENTS.md) | Agent 如何工作：权限、协作、变更、测试、提交和交付规则 | 产品功能清单、当前进度、历史完成结论 |
| [PRODUCT.md](PRODUCT.md) | 产品要满足什么：目标、范围、业务规则、体验、验收和待决定事项 | 技术方案、任务排期、测试运行结果 |
| [PROJECT_LOG.md](PROJECT_LOG.md) | 实际做到哪里：当前状态、任务依赖、决定来源、验证证据、阻塞和下一步 | 另一套产品要求、另一套 Agent 工作规则 |

- 使用以上精确文件名，不另建 `agent.md`、`product.md` 或 `product log.md` 等重复入口。
- 用户最新明确指令在既有授权范围内优先执行，并同步对应文档。产品语义以 PRODUCT 当前正文为准；执行状态以日志当前投影和可核验代码、测试、Git 证据为准；证据不支持时标为未验证或失败。
- 技术方案与 evidence 附件用于解释设计、保存证据；执行状态统一由 PROJECT_LOG 引用维护，不另建动态进度账本，也不能把建议变成已确认需求。历史快照只供追溯，不能覆盖当前三份文档。

## 与用户的交互方式

- 默认用中文和产品语言沟通，先说明用户能得到什么，再解释必要的理由；不把工程说明塞进用户使用界面。
- 用户提出“帮我”“修改”“继续”等行动请求时，直接推进已授权的工作，不停在能力介绍、计划或反复询问“是否继续”。
- 主动把零散想法整理成目标、范围和可观察的验收结果；小任务直接执行，信息不足但可安全推进时说明假设并继续。技术方案、工具和实现由 AI 决定，不把技术选择推给用户。
- 只追问会实质改变产品目标、验收或授权边界的问题，并说明需要用户决定的原因；已明确授权的操作不重复确认。等待答案时继续不依赖该答案的安全工作。
- 在开始、关键进展、风险、阻塞和交付时简短汇报；持续执行期间不让用户长时间不知道状态，不逐条播报内部命令。区分事实、推断和未验证事项，不以测试数量替代产品验收。
- 用户中途补充要求时，先判断是修正、追问还是替换目标；回答状态问题后继续原任务，避免丢掉尚未完成的目标。用户明确要求停止时，立即停止当前任务及其子任务的执行，不继续写入或自动下一步。
- 交付说明结果、验证、剩余限制和实际 commit SHA，文件给出可点击的绝对路径；需要用户验收时说明具体操作和预期结果，不将工程通过当作用户已认可。

## 恢复与持续执行

1. 先读本规则、PRODUCT、PROJECT_LOG 当前投影及最新记录；核对 Git 分支、HEAD、暂存区、未提交修改和与本任务相关的实际代码。
2. 涉及规划、开发、修改、修复或审查时使用 `teacher-product-development` Skill。先说明目标、范围、验收与必要假设；明确的小任务直接执行。
3. 用户决定产品目标和关键边界；AI 决定技术方案、工具、实现和验证。已授权、范围内、可逆的技术工作持续推进，不反复询问是否继续。
4. 仅追问会改变产品语义、验收或授权边界的事项。将产品待决定项集中登记在 PRODUCT；日志引用其编号、影响任务及最迟决定阶段，其他独立工作继续。
5. 复杂任务按依赖组织多 Agent；任务书明确上下文、文件归属、输入输出和验收。同阶段同一文件只有一名写入负责人；主 Agent 整合并实际核验证据，关键成果安排独立审查。

## 长任务连续推进

- 以用户授权的整体目标和验收条件作为结束依据；单个工作包、测试通过、commit 或子 Agent 完成都不是长任务结束条件。只修改规则、评估或回答问题，不自动启动文档中列出的业务长任务。
- 每个工作包按门禁完成后：核验成果 → 更新日志 → 提交 commit → 汇报阶段进度 → 选择并立即执行下一个已授权且依赖满足的任务。阶段汇报使用进度消息，不以最终回复结束执行或等待用户说“继续”。
- 子 Agent 完成后，主 Agent 必须检查、整合和安排后续任务；发现缺陷应修复、重派或接手，不能只转述结果后收工。
- 局部阻塞只暂停受影响任务；记录解除条件，继续其他已授权且依赖满足的任务。计划中的真实服务、费用、凭据和资料边界仍然有效，不以连续推进扩大授权。
- 最终回复前检查剩余任务：仍有可安全推进的计划内任务就继续；仅在整体目标达到验收、用户要求停止，或剩余工作均需用户决定/外部条件且无独立可执行任务时结束，并说明依据。
- 日志当前投影维护“长任务目标及结束条件”“当前可执行任务”“被阻塞任务及解除条件”；任务状态变化时同步，恢复时核对实际证据。执行意外中断时尽可能保存明确的续接点；文档规则本身不提供自动唤醒能力。

## 变更如何流转

- 产品目标、功能或业务规则发生变化：先同步 PRODUCT 对应条款、待决定状态和版本记录，再在日志记录用户决定来源、影响任务和验收变化；不能从旧设计或实现反推需求。
- 技术实现、修复、测试或配置变化：更新实现及相关测试，在日志绑定需求编号和任务编号；需求未变时不为每个 commit 升级产品版本。
- 工作流程变化：更新本文件和相关治理检查，在日志记录原因与验证。文档整理、错字或待决定项登记不自动升级已确认产品范围。
- 每次完整任务变更采用稳定任务编号串联需求、测试、证据和 commit；简单任务只需一条日志，不强制新增方案文件。

## 测试与交付门禁

- 每次改动都执行与风险相称的验证。业务代码按改变的行为、失败与回归风险补充相关测试；文档或规则变化检查一致性、引用和状态。低影响文档改动可直接检查，不为验证文字而引入测试框架或无关断言。
- 交付前必须完成本任务全部必需检查。项目建立统一自动化入口后，运行该入口及本任务所需的实际交互、迁移等验收，不用局部测试替代必需的完整门禁。当前可用检查入口和不适用项由 PROJECT_LOG 记录；不得照搬其他项目的命令或虚报不存在的检查已通过。
- 需要数据库测试时使用隔离测试环境和合成资料；不能复用真实数据库、资料或其他项目密钥。先做聚焦检查，失败修复后再收口全量验证。
- 在日志记录新增或更新的测试、实际命令、环境、结果、失败/跳过项和证据位置。历史结果与子 Agent 自报不能当成本次通过证据；发现跳过项要解释原因，所需验收未执行就不能标为通过。
- 自动化工程验证、真实模型效果、真实渠道/设备、用户交互验收和发布分别记录。未授权或条件不具备的真实验证记为未验证，不得用模拟通过替代。
- 任一必须检查失败或未执行，任务不能标为已完成或交付通过。先在范围内修复；超出范围或存在不可安全解决的阻塞时如实报告，不删除测试、降低断言、放宽门禁或修改历史证据来制造通过。

## Git 提交与回滚追踪

- 每个新项目从创建开始必须纳入 Git 版本管理。先确认项目目录和仓库归属，使用 `git rev-parse --show-toplevel` 核对是否属于已有仓库；独立新项目尚无仓库时，在其根目录执行 `git init`，不能仅因父目录有仓库就误认项目已正确建仓。
- 已明确属于现有仓库的项目或模块沿用该仓库，保留历史，不重复初始化或随意创建嵌套仓库；当前教研联盟管理平台沿用现有 Git 仓库。
- 新项目先设置合适的 `.gitignore` 和测试验证入口，完成首批验证后创建初始 commit。不得把依赖、构建产物、密钥或真实学校、教师、学生资料提交到 Git；初始提交记录可用的项目骨架、规则、需求、日志和相关测试，验证阻塞时遵循下方 checkpoint 规则。
- 当前项目已经是独立 Git 仓库，沿用现有历史。每次完成一项可独立描述的改动，都必须创建对应的 Git commit 后再交付；无需为每一次文件保存提交。
- 正常顺序：改动与相应检查 → 全部必需验证通过 → 同步日志 → 审查暂存差异 → 创建 commit → 核对 SHA 与剩余工作区 → 工作包检查点；随后按长任务规则继续调度，达到整体结束条件才最终交付。验证后再次改代码或测试，必须重跑受影响检查；最终文档也要完成一致性和引用检查。
- commit message 使用 `类型(任务编号): [xiaosi] 具体变化`，例如 `docs(GOV-001): [xiaosi] clarify project document ownership`。用户要求提交记录带上 `xiaosi`，后续每次提交的标题均须包含该标记，便于检索追踪。日志记录相同任务编号和验证证据，通过 `git log --all --fixed-strings --grep='(GOV-001)'` 查找提交；最终交付提供真实 SHA。不要为了把同次提交的 SHA 写回日志而无限补提交。
- 提交前核对已有暂存内容与文件归属，只提交本任务范围内的改动；不使用无差别 `git add .`，不撤销或夹带其他 Agent、用户的无关修改。若同一文档已有相关未提交内容，保留并说明本次包含的文档基线。
- 如为满足每次改动留痕而在验证阻塞时保存 commit，只能明确标注为 `checkpoint(任务编号)`，在日志和交付说明中写明失败/未验证；这不是合格交付，也不是已验证可用的回滚点。
- 本地 commit 不代表 push、上传、部署或发布。版本追踪不自动授权远端写入、强制覆盖或数据回滚；包含迁移时另记兼容与恢复条件，不能用代码回滚冒充数据恢复。

## 授权与已有成果保护

- 本地合成环境中的可逆工作沿用用户既有授权。产品候选功能不自动授权真实凭据、资料传输、费用、外部写入或部署；当前运行边界见 PROJECT_LOG。已明确授权的 GitHub 同步无需重复确认，不能由此推导出公开仓库、强制推送、上传真实数据或部署权限。
- 保留用户和其他 Agent 的已有修改、业务资料及历史。大范围删除先核对清单并在源码树外留可恢复备份；不移动或删除旧工作空间来源。
- 其他项目的业务功能、UI、截图、历史设计和授权不作为本项目默认依据。采用用户指定参考时只迁移本次需要的规则或内容，参考来源及实际验收状态由日志记录。
